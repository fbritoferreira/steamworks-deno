// GENERATED by @steamworks/codegen from steam_api.json (SDK 1.65). Do not edit.

import { PACK, readArray, write } from "../../src/layout.ts";
import type { CallResultHost } from "../../src/marshal.ts";
import {
  arrayOut,
  cstrArg,
  outString,
  readOutString,
  readScalar,
  readScalarArray,
  scalarOut,
  writeScalarArray,
} from "../../src/marshal.ts";
import {
  decodeSteamInventoryEligiblePromoItemDefIDs_t,
  decodeSteamInventoryRequestPricesResult_t,
  decodeSteamInventoryStartPurchaseResult_t,
  decodeSteamItemDetails_t,
  SteamInventoryEligiblePromoItemDefIDs_t,
  SteamInventoryRequestPricesResult_t,
  SteamInventoryStartPurchaseResult_t,
  SteamItemDetails_t,
  SteamItemDetails_t_layout,
} from "../structs.ts";
import type { EResult } from "../enums.ts";

export const ISteamInventory_symbols = {
  SteamAPI_ISteamInventory_GetResultStatus: { parameters: ["pointer", "i32"], result: "i32" },
  SteamAPI_ISteamInventory_GetResultItems: {
    parameters: ["pointer", "i32", "buffer", "buffer"],
    result: "bool",
  },
  SteamAPI_ISteamInventory_GetResultItemProperty: {
    parameters: ["pointer", "i32", "u32", "buffer", "buffer", "buffer"],
    result: "bool",
  },
  SteamAPI_ISteamInventory_GetResultTimestamp: { parameters: ["pointer", "i32"], result: "u32" },
  SteamAPI_ISteamInventory_CheckResultSteamID: {
    parameters: ["pointer", "i32", "u64"],
    result: "bool",
  },
  SteamAPI_ISteamInventory_DestroyResult: { parameters: ["pointer", "i32"], result: "void" },
  SteamAPI_ISteamInventory_GetAllItems: { parameters: ["pointer", "buffer"], result: "bool" },
  SteamAPI_ISteamInventory_GetItemsByID: {
    parameters: ["pointer", "buffer", "buffer", "u32"],
    result: "bool",
  },
  SteamAPI_ISteamInventory_SerializeResult: {
    parameters: ["pointer", "i32", "buffer", "buffer"],
    result: "bool",
  },
  SteamAPI_ISteamInventory_DeserializeResult: {
    parameters: ["pointer", "buffer", "buffer", "u32", "bool"],
    result: "bool",
  },
  SteamAPI_ISteamInventory_GenerateItems: {
    parameters: ["pointer", "buffer", "buffer", "buffer", "u32"],
    result: "bool",
  },
  SteamAPI_ISteamInventory_GrantPromoItems: { parameters: ["pointer", "buffer"], result: "bool" },
  SteamAPI_ISteamInventory_AddPromoItem: {
    parameters: ["pointer", "buffer", "i32"],
    result: "bool",
  },
  SteamAPI_ISteamInventory_AddPromoItems: {
    parameters: ["pointer", "buffer", "buffer", "u32"],
    result: "bool",
  },
  SteamAPI_ISteamInventory_ConsumeItem: {
    parameters: ["pointer", "buffer", "u64", "u32"],
    result: "bool",
  },
  SteamAPI_ISteamInventory_ExchangeItems: {
    parameters: ["pointer", "buffer", "buffer", "buffer", "u32", "buffer", "buffer", "u32"],
    result: "bool",
  },
  SteamAPI_ISteamInventory_TransferItemQuantity: {
    parameters: ["pointer", "buffer", "u64", "u32", "u64"],
    result: "bool",
  },
  SteamAPI_ISteamInventory_SendItemDropHeartbeat: { parameters: ["pointer"], result: "void" },
  SteamAPI_ISteamInventory_TriggerItemDrop: {
    parameters: ["pointer", "buffer", "i32"],
    result: "bool",
  },
  SteamAPI_ISteamInventory_TradeItems: {
    parameters: ["pointer", "buffer", "u64", "buffer", "buffer", "u32", "buffer", "buffer", "u32"],
    result: "bool",
  },
  SteamAPI_ISteamInventory_LoadItemDefinitions: { parameters: ["pointer"], result: "bool" },
  SteamAPI_ISteamInventory_GetItemDefinitionIDs: {
    parameters: ["pointer", "buffer", "buffer"],
    result: "bool",
  },
  SteamAPI_ISteamInventory_GetItemDefinitionProperty: {
    parameters: ["pointer", "i32", "buffer", "buffer", "buffer"],
    result: "bool",
  },
  SteamAPI_ISteamInventory_RequestEligiblePromoItemDefinitionsIDs: {
    parameters: ["pointer", "u64"],
    result: "u64",
  },
  SteamAPI_ISteamInventory_GetEligiblePromoItemDefinitionIDs: {
    parameters: ["pointer", "u64", "buffer", "buffer"],
    result: "bool",
  },
  SteamAPI_ISteamInventory_StartPurchase: {
    parameters: ["pointer", "buffer", "buffer", "u32"],
    result: "u64",
  },
  SteamAPI_ISteamInventory_RequestPrices: { parameters: ["pointer"], result: "u64" },
  SteamAPI_ISteamInventory_GetNumItemsWithPrices: { parameters: ["pointer"], result: "u32" },
  SteamAPI_ISteamInventory_GetItemsWithPrices: {
    parameters: ["pointer", "buffer", "buffer", "buffer", "u32"],
    result: "bool",
  },
  SteamAPI_ISteamInventory_GetItemPrice: {
    parameters: ["pointer", "i32", "buffer", "buffer"],
    result: "bool",
  },
  SteamAPI_ISteamInventory_StartUpdateProperties: { parameters: ["pointer"], result: "u64" },
  SteamAPI_ISteamInventory_RemoveProperty: {
    parameters: ["pointer", "u64", "u64", "buffer"],
    result: "bool",
  },
  SteamAPI_ISteamInventory_SetPropertyString: {
    parameters: ["pointer", "u64", "u64", "buffer", "buffer"],
    result: "bool",
  },
  SteamAPI_ISteamInventory_SetPropertyBool: {
    parameters: ["pointer", "u64", "u64", "buffer", "bool"],
    result: "bool",
  },
  SteamAPI_ISteamInventory_SetPropertyInt64: {
    parameters: ["pointer", "u64", "u64", "buffer", "i64"],
    result: "bool",
  },
  SteamAPI_ISteamInventory_SetPropertyFloat: {
    parameters: ["pointer", "u64", "u64", "buffer", "f32"],
    result: "bool",
  },
  SteamAPI_ISteamInventory_SubmitUpdateProperties: {
    parameters: ["pointer", "u64", "buffer"],
    result: "bool",
  },
  SteamAPI_ISteamInventory_InspectItem: {
    parameters: ["pointer", "buffer", "buffer"],
    result: "bool",
  },
  SteamAPI_SteamInventory_v003: { parameters: [], result: "pointer", optional: true },
} as const satisfies Deno.ForeignLibraryInterface;

export class ISteamInventory {
  static readonly accessor = "SteamAPI_SteamInventory_v003";

  constructor(
    private readonly s: Deno.DynamicLibrary<typeof ISteamInventory_symbols>["symbols"],
    private readonly self: Deno.PointerValue,
    private readonly host: CallResultHost,
  ) {}

  /**
   * Find out the status of an asynchronous inventory result handle. Possible values:
   * k_EResultPending - still in progress
   * k_EResultOK - done, result ready
   * k_EResultExpired - done, result ready, maybe out of date (see DeserializeResult)
   * k_EResultInvalidParam - ERROR: invalid API call parameters
   * k_EResultServiceUnavailable - ERROR: service temporarily down, you may retry later
   * k_EResultLimitExceeded - ERROR: operation would exceed per-user inventory limits
   * k_EResultFail - ERROR: unknown / generic error
   */
  getResultStatus(resultHandle: number): EResult {
    return this.s.SteamAPI_ISteamInventory_GetResultStatus(this.self, resultHandle) as EResult;
  }

  /**
   * Copies the contents of a result set into a flat array. The specific
   * contents of the result set depend on which query which was used.
   */
  getResultItems(resultHandle: number, punOutItemsArraySize: number): SteamItemDetails_t[] {
    const pOutItemsArray_buf = new Uint8Array(
      SteamItemDetails_t_layout[PACK].size * punOutItemsArraySize,
    );
    const punOutItemsArraySize_buf = scalarOut("u32");
    write.u32(punOutItemsArraySize_buf, 0, punOutItemsArraySize);
    this.s.SteamAPI_ISteamInventory_GetResultItems(
      this.self,
      resultHandle,
      pOutItemsArray_buf,
      punOutItemsArraySize_buf,
    );
    const n = punOutItemsArraySize;
    return readArray(
      pOutItemsArray_buf,
      0,
      n,
      SteamItemDetails_t_layout[PACK].size,
      (b, o) => decodeSteamItemDetails_t(b.subarray(o, o + SteamItemDetails_t_layout[PACK].size)),
    );
  }

  /**
   * In combination with GetResultItems, you can use GetResultItemProperty to retrieve
   * dynamic string properties for a given item returned in the result set.
   * Property names are always composed of ASCII letters, numbers, and/or underscores.
   * Pass a NULL pointer for pchPropertyName to get a comma - separated list of available
   * property names.
   * If pchValueBuffer is NULL, *punValueBufferSize will contain the
   * suggested buffer size. Otherwise it will be the number of bytes actually copied
   * to pchValueBuffer. If the results do not fit in the given buffer, partial
   * results may be copied.
   */
  getResultItemProperty(
    resultHandle: number,
    unItemIndex: number,
    pchPropertyName: string,
    punValueBufferSizeOut: number,
  ): { ok: boolean; pchValueBuffer: string } {
    const pchValueBuffer_buf = outString(punValueBufferSizeOut);
    const punValueBufferSizeOut_buf = scalarOut("u32");
    write.u32(punValueBufferSizeOut_buf, 0, punValueBufferSizeOut);
    const ok = this.s.SteamAPI_ISteamInventory_GetResultItemProperty(
      this.self,
      resultHandle,
      unItemIndex,
      cstrArg(pchPropertyName),
      pchValueBuffer_buf,
      punValueBufferSizeOut_buf,
    );
    return { ok, pchValueBuffer: readOutString(pchValueBuffer_buf) };
  }

  /**
   * Returns the server time at which the result was generated. Compare against
   * the value of IClientUtils::GetServerRealTime() to determine age.
   */
  getResultTimestamp(resultHandle: number): number {
    return this.s.SteamAPI_ISteamInventory_GetResultTimestamp(this.self, resultHandle);
  }

  /**
   * Returns true if the result belongs to the target steam ID, false if the
   * result does not. This is important when using DeserializeResult, to verify
   * that a remote player is not pretending to have a different user's inventory.
   */
  checkResultSteamID(resultHandle: number, steamIDExpected: bigint): boolean {
    return this.s.SteamAPI_ISteamInventory_CheckResultSteamID(
      this.self,
      resultHandle,
      steamIDExpected,
    );
  }

  /** Destroys a result handle and frees all associated memory. */
  destroyResult(resultHandle: number): void {
    this.s.SteamAPI_ISteamInventory_DestroyResult(this.self, resultHandle);
  }

  /**
   * Captures the entire state of the current user's Steam inventory.
   * You must call DestroyResult on this handle when you are done with it.
   * Returns false and sets *pResultHandle to zero if inventory is unavailable.
   * Note: calls to this function are subject to rate limits and may return
   * cached results if called too frequently. It is suggested that you call
   * this function only when you are about to display the user's full inventory,
   * or if you expect that the inventory may have changed.
   */
  getAllItems(): { ok: boolean; pResultHandle: number } {
    const pResultHandle_buf = scalarOut("i32");
    const ok = this.s.SteamAPI_ISteamInventory_GetAllItems(this.self, pResultHandle_buf);
    return { ok, pResultHandle: readScalar(pResultHandle_buf, "i32") as number };
  }

  /**
   * Captures the state of a subset of the current user's Steam inventory,
   * identified by an array of item instance IDs. The results from this call
   * can be serialized and passed to other players to "prove" that the current
   * user owns specific items, without exposing the user's entire inventory.
   * For example, you could call GetItemsByID with the IDs of the user's
   * currently equipped cosmetic items and serialize this to a buffer, and
   * then transmit this buffer to other players upon joining a game.
   */
  getItemsByID(
    pInstanceIDs: bigint[],
    unCountInstanceIDs: number,
  ): { ok: boolean; pResultHandle: number } {
    const pResultHandle_buf = scalarOut("i32");
    const ok = this.s.SteamAPI_ISteamInventory_GetItemsByID(
      this.self,
      pResultHandle_buf,
      writeScalarArray("u64", pInstanceIDs),
      unCountInstanceIDs,
    );
    return { ok, pResultHandle: readScalar(pResultHandle_buf, "i32") as number };
  }

  /**
   * Serializes a result set with signature bytes to an output buffer. Pass
   * NULL as an output buffer to get the required size via punOutBufferSize.
   * The size of a serialized result depends on the number items which are being
   * serialized. When securely transmitting items to other players, it is
   * recommended to use "GetItemsByID" first to create a minimal result set.
   * Results have a built-in timestamp which will be considered "expired" after
   * an hour has elapsed. See DeserializeResult for expiration handling.
   */
  serializeResult(resultHandle: number, pOutBuffer: Uint8Array, punOutBufferSize: number): boolean {
    const punOutBufferSize_buf = scalarOut("u32");
    write.u32(punOutBufferSize_buf, 0, punOutBufferSize);
    return this.s.SteamAPI_ISteamInventory_SerializeResult(
      this.self,
      resultHandle,
      pOutBuffer,
      punOutBufferSize_buf,
    );
  }

  /**
   * Deserializes a result set and verifies the signature bytes. Returns false
   * if bRequireFullOnlineVerify is set but Steam is running in Offline mode.
   * Otherwise returns true and then delivers error codes via GetResultStatus.
   * The bRESERVED_MUST_BE_FALSE flag is reserved for future use and should not
   * be set to true by your game at this time.
   * DeserializeResult has a potential soft-failure mode where the handle status
   * is set to k_EResultExpired. GetResultItems() still succeeds in this mode.
   * The "expired" result could indicate that the data may be out of date - not
   * just due to timed expiration (one hour), but also because one of the items
   * in the result set may have been traded or consumed since the result set was
   * generated. You could compare the timestamp from GetResultTimestamp() to
   * ISteamUtils::GetServerRealTime() to determine how old the data is. You could
   * simply ignore the "expired" result code and continue as normal, or you
   * could challenge the player with expired data to send an updated result set.
   */
  deserializeResult(
    pBuffer: Uint8Array,
    unBufferSize: number,
    bRESERVED_MUST_BE_FALSE: boolean,
  ): { ok: boolean; pOutResultHandle: number } {
    const pOutResultHandle_buf = scalarOut("i32");
    const ok = this.s.SteamAPI_ISteamInventory_DeserializeResult(
      this.self,
      pOutResultHandle_buf,
      pBuffer,
      unBufferSize,
      bRESERVED_MUST_BE_FALSE,
    );
    return { ok, pOutResultHandle: readScalar(pOutResultHandle_buf, "i32") as number };
  }

  /**
   * GenerateItems() creates one or more items and then generates a SteamInventoryCallback_t
   * notification with a matching nCallbackContext parameter. This API is only intended
   * for prototyping - it is only usable by Steam accounts that belong to the publisher group
   * for your game.
   * If punArrayQuantity is not NULL, it should be the same length as pArrayItems and should
   * describe the quantity of each item to generate.
   */
  generateItems(
    pArrayItemDefs: number[],
    punArrayQuantity: number[],
    unArrayLength: number,
  ): { ok: boolean; pResultHandle: number } {
    const pResultHandle_buf = scalarOut("i32");
    const ok = this.s.SteamAPI_ISteamInventory_GenerateItems(
      this.self,
      pResultHandle_buf,
      writeScalarArray("i32", pArrayItemDefs),
      writeScalarArray("u32", punArrayQuantity),
      unArrayLength,
    );
    return { ok, pResultHandle: readScalar(pResultHandle_buf, "i32") as number };
  }

  /**
   * GrantPromoItems() checks the list of promotional items for which the user may be eligible
   * and grants the items (one time only).  On success, the result set will include items which
   * were granted, if any. If no items were granted because the user isn't eligible for any
   * promotions, this is still considered a success.
   */
  grantPromoItems(): { ok: boolean; pResultHandle: number } {
    const pResultHandle_buf = scalarOut("i32");
    const ok = this.s.SteamAPI_ISteamInventory_GrantPromoItems(this.self, pResultHandle_buf);
    return { ok, pResultHandle: readScalar(pResultHandle_buf, "i32") as number };
  }

  /**
   * AddPromoItem() / AddPromoItems() are restricted versions of GrantPromoItems(). Instead of
   * scanning for all eligible promotional items, the check is restricted to a single item
   * definition or set of item definitions. This can be useful if your game has custom UI for
   * showing a specific promo item to the user.
   */
  addPromoItem(itemDef: number): { ok: boolean; pResultHandle: number } {
    const pResultHandle_buf = scalarOut("i32");
    const ok = this.s.SteamAPI_ISteamInventory_AddPromoItem(this.self, pResultHandle_buf, itemDef);
    return { ok, pResultHandle: readScalar(pResultHandle_buf, "i32") as number };
  }

  addPromoItems(
    pArrayItemDefs: number[],
    unArrayLength: number,
  ): { ok: boolean; pResultHandle: number } {
    const pResultHandle_buf = scalarOut("i32");
    const ok = this.s.SteamAPI_ISteamInventory_AddPromoItems(
      this.self,
      pResultHandle_buf,
      writeScalarArray("i32", pArrayItemDefs),
      unArrayLength,
    );
    return { ok, pResultHandle: readScalar(pResultHandle_buf, "i32") as number };
  }

  /**
   * ConsumeItem() removes items from the inventory, permanently. They cannot be recovered.
   * Not for the faint of heart - if your game implements item removal at all, a high-friction
   * UI confirmation process is highly recommended.
   */
  consumeItem(itemConsume: bigint, unQuantity: number): { ok: boolean; pResultHandle: number } {
    const pResultHandle_buf = scalarOut("i32");
    const ok = this.s.SteamAPI_ISteamInventory_ConsumeItem(
      this.self,
      pResultHandle_buf,
      itemConsume,
      unQuantity,
    );
    return { ok, pResultHandle: readScalar(pResultHandle_buf, "i32") as number };
  }

  /**
   * ExchangeItems() is an atomic combination of item generation and consumption.
   * It can be used to implement crafting recipes or transmutations, or items which unpack
   * themselves into other items (e.g., a chest).
   * Exchange recipes are defined in the ItemDef, and explicitly list the required item
   * types and resulting generated type.
   * Exchange recipes are evaluated atomically by the Inventory Service; if the supplied
   * components do not match the recipe, or do not contain sufficient quantity, the
   * exchange will fail.
   */
  exchangeItems(
    pArrayGenerate: number[],
    punArrayGenerateQuantity: number[],
    unArrayGenerateLength: number,
    pArrayDestroy: bigint[],
    punArrayDestroyQuantity: number[],
    unArrayDestroyLength: number,
  ): { ok: boolean; pResultHandle: number } {
    const pResultHandle_buf = scalarOut("i32");
    const ok = this.s.SteamAPI_ISteamInventory_ExchangeItems(
      this.self,
      pResultHandle_buf,
      writeScalarArray("i32", pArrayGenerate),
      writeScalarArray("u32", punArrayGenerateQuantity),
      unArrayGenerateLength,
      writeScalarArray("u64", pArrayDestroy),
      writeScalarArray("u32", punArrayDestroyQuantity),
      unArrayDestroyLength,
    );
    return { ok, pResultHandle: readScalar(pResultHandle_buf, "i32") as number };
  }

  /**
   * TransferItemQuantity() is intended for use with items which are "stackable" (can have
   * quantity greater than one). It can be used to split a stack into two, or to transfer
   * quantity from one stack into another stack of identical items. To split one stack into
   * two, pass k_SteamItemInstanceIDInvalid for itemIdDest and a new item will be generated.
   */
  transferItemQuantity(
    itemIdSource: bigint,
    unQuantity: number,
    itemIdDest: bigint,
  ): { ok: boolean; pResultHandle: number } {
    const pResultHandle_buf = scalarOut("i32");
    const ok = this.s.SteamAPI_ISteamInventory_TransferItemQuantity(
      this.self,
      pResultHandle_buf,
      itemIdSource,
      unQuantity,
      itemIdDest,
    );
    return { ok, pResultHandle: readScalar(pResultHandle_buf, "i32") as number };
  }

  /** Deprecated. Calling this method is not required for proper playtime accounting. */
  sendItemDropHeartbeat(): void {
    this.s.SteamAPI_ISteamInventory_SendItemDropHeartbeat(this.self);
  }

  /**
   * Playtime credit must be consumed and turned into item drops by your game. Only item
   * definitions which are marked as "playtime item generators" can be spawned. The call
   * will return an empty result set if there is not enough playtime credit for a drop.
   * Your game should call TriggerItemDrop at an appropriate time for the user to receive
   * new items, such as between rounds or while the player is dead. Note that players who
   * hack their clients could modify the value of "dropListDefinition", so do not use it
   * to directly control rarity.
   * See your Steamworks configuration to set playtime drop rates for individual itemdefs.
   * The client library will suppress too-frequent calls to this method.
   */
  triggerItemDrop(dropListDefinition: number): { ok: boolean; pResultHandle: number } {
    const pResultHandle_buf = scalarOut("i32");
    const ok = this.s.SteamAPI_ISteamInventory_TriggerItemDrop(
      this.self,
      pResultHandle_buf,
      dropListDefinition,
    );
    return { ok, pResultHandle: readScalar(pResultHandle_buf, "i32") as number };
  }

  /** Deprecated. This method is not supported. */
  tradeItems(
    steamIDTradePartner: bigint,
    pArrayGive: bigint[],
    pArrayGiveQuantity: number[],
    nArrayGiveLength: number,
    pArrayGet: bigint[],
    pArrayGetQuantity: number[],
    nArrayGetLength: number,
  ): { ok: boolean; pResultHandle: number } {
    const pResultHandle_buf = scalarOut("i32");
    const ok = this.s.SteamAPI_ISteamInventory_TradeItems(
      this.self,
      pResultHandle_buf,
      steamIDTradePartner,
      writeScalarArray("u64", pArrayGive),
      writeScalarArray("u32", pArrayGiveQuantity),
      nArrayGiveLength,
      writeScalarArray("u64", pArrayGet),
      writeScalarArray("u32", pArrayGetQuantity),
      nArrayGetLength,
    );
    return { ok, pResultHandle: readScalar(pResultHandle_buf, "i32") as number };
  }

  /**
   * LoadItemDefinitions triggers the automatic load and refresh of item definitions.
   * Every time new item definitions are available (eg, from the dynamic addition of new
   * item types while players are still in-game), a SteamInventoryDefinitionUpdate_t
   * callback will be fired.
   */
  loadItemDefinitions(): boolean {
    return this.s.SteamAPI_ISteamInventory_LoadItemDefinitions(this.self);
  }

  /**
   * GetItemDefinitionIDs returns the set of all defined item definition IDs (which are
   * defined via Steamworks configuration, and not necessarily contiguous integers).
   * If pItemDefIDs is null, the call will return true and *punItemDefIDsArraySize will
   * contain the total size necessary for a subsequent call. Otherwise, the call will
   * return false if and only if there is not enough space in the output array.
   */
  getItemDefinitionIDs(punItemDefIDsArraySize: number): number[] {
    const pItemDefIDs_buf = arrayOut("i32", punItemDefIDsArraySize);
    const punItemDefIDsArraySize_buf = scalarOut("u32");
    write.u32(punItemDefIDsArraySize_buf, 0, punItemDefIDsArraySize);
    this.s.SteamAPI_ISteamInventory_GetItemDefinitionIDs(
      this.self,
      pItemDefIDs_buf,
      punItemDefIDsArraySize_buf,
    );
    const n = punItemDefIDsArraySize;
    return readScalarArray(pItemDefIDs_buf, "i32", n) as number[];
  }

  /**
   * GetItemDefinitionProperty returns a string property from a given item definition.
   * Note that some properties (for example, "name") may be localized and will depend
   * on the current Steam language settings (see ISteamApps::GetCurrentGameLanguage).
   * Property names are always composed of ASCII letters, numbers, and/or underscores.
   * Pass a NULL pointer for pchPropertyName to get a comma - separated list of available
   * property names. If pchValueBuffer is NULL, *punValueBufferSize will contain the
   * suggested buffer size. Otherwise it will be the number of bytes actually copied
   * to pchValueBuffer. If the results do not fit in the given buffer, partial
   * results may be copied.
   */
  getItemDefinitionProperty(
    iDefinition: number,
    pchPropertyName: string,
    punValueBufferSizeOut: number,
  ): { ok: boolean; pchValueBuffer: string } {
    const pchValueBuffer_buf = outString(punValueBufferSizeOut);
    const punValueBufferSizeOut_buf = scalarOut("u32");
    write.u32(punValueBufferSizeOut_buf, 0, punValueBufferSizeOut);
    const ok = this.s.SteamAPI_ISteamInventory_GetItemDefinitionProperty(
      this.self,
      iDefinition,
      cstrArg(pchPropertyName),
      pchValueBuffer_buf,
      punValueBufferSizeOut_buf,
    );
    return { ok, pchValueBuffer: readOutString(pchValueBuffer_buf) };
  }

  /**
   * Request the list of "eligible" promo items that can be manually granted to the given
   * user.  These are promo items of type "manual" that won't be granted automatically.
   * An example usage of this is an item that becomes available every week.
   */
  requestEligiblePromoItemDefinitionsIDs(
    steamID: bigint,
  ): Promise<SteamInventoryEligiblePromoItemDefIDs_t> {
    const call = this.s.SteamAPI_ISteamInventory_RequestEligiblePromoItemDefinitionsIDs(
      this.self,
      steamID,
    );
    return this.host.callResult(call, 4703, decodeSteamInventoryEligiblePromoItemDefIDs_t);
  }

  /**
   * After handling a SteamInventoryEligiblePromoItemDefIDs_t call result, use this
   * function to pull out the list of item definition ids that the user can be
   * manually granted via the AddPromoItems() call.
   */
  getEligiblePromoItemDefinitionIDs(steamID: bigint, punItemDefIDsArraySize: number): number[] {
    const pItemDefIDs_buf = arrayOut("i32", punItemDefIDsArraySize);
    const punItemDefIDsArraySize_buf = scalarOut("u32");
    write.u32(punItemDefIDsArraySize_buf, 0, punItemDefIDsArraySize);
    this.s.SteamAPI_ISteamInventory_GetEligiblePromoItemDefinitionIDs(
      this.self,
      steamID,
      pItemDefIDs_buf,
      punItemDefIDsArraySize_buf,
    );
    const n = punItemDefIDsArraySize;
    return readScalarArray(pItemDefIDs_buf, "i32", n) as number[];
  }

  /**
   * Starts the purchase process for the given item definitions.  The callback SteamInventoryStartPurchaseResult_t
   * will be posted if Steam was able to initialize the transaction.
   * Once the purchase has been authorized and completed by the user, the callback SteamInventoryResultReady_t
   * will be posted.
   */
  startPurchase(
    pArrayItemDefs: number[],
    punArrayQuantity: number[],
    unArrayLength: number,
  ): Promise<SteamInventoryStartPurchaseResult_t> {
    const call = this.s.SteamAPI_ISteamInventory_StartPurchase(
      this.self,
      writeScalarArray("i32", pArrayItemDefs),
      writeScalarArray("u32", punArrayQuantity),
      unArrayLength,
    );
    return this.host.callResult(call, 4704, decodeSteamInventoryStartPurchaseResult_t);
  }

  /** Request current prices for all applicable item definitions */
  requestPrices(): Promise<SteamInventoryRequestPricesResult_t> {
    const call = this.s.SteamAPI_ISteamInventory_RequestPrices(this.self);
    return this.host.callResult(call, 4705, decodeSteamInventoryRequestPricesResult_t);
  }

  /** Returns the number of items with prices.  Need to call RequestPrices() first. */
  getNumItemsWithPrices(): number {
    return this.s.SteamAPI_ISteamInventory_GetNumItemsWithPrices(this.self);
  }

  /**
   * Returns item definition ids and their prices in the user's local currency.
   * Need to call RequestPrices() first.
   */
  getItemsWithPrices(unArrayLength: number): bigint[] {
    const pArrayItemDefs_buf = arrayOut("i32", unArrayLength);
    const pCurrentPrices_buf = arrayOut("u64", unArrayLength);
    const pBasePrices_buf = arrayOut("u64", unArrayLength);
    this.s.SteamAPI_ISteamInventory_GetItemsWithPrices(
      this.self,
      pArrayItemDefs_buf,
      pCurrentPrices_buf,
      pBasePrices_buf,
      unArrayLength,
    );
    const n = unArrayLength;
    return readScalarArray(pBasePrices_buf, "u64", n) as bigint[];
  }

  /**
   * Retrieves the price for the item definition id
   * Returns false if there is no price stored for the item definition.
   */
  getItemPrice(iDefinition: number): { ok: boolean; pCurrentPrice: bigint; pBasePrice: bigint } {
    const pCurrentPrice_buf = scalarOut("u64");
    const pBasePrice_buf = scalarOut("u64");
    const ok = this.s.SteamAPI_ISteamInventory_GetItemPrice(
      this.self,
      iDefinition,
      pCurrentPrice_buf,
      pBasePrice_buf,
    );
    return {
      ok,
      pCurrentPrice: readScalar(pCurrentPrice_buf, "u64") as bigint,
      pBasePrice: readScalar(pBasePrice_buf, "u64") as bigint,
    };
  }

  /** Create a request to update properties on items */
  startUpdateProperties(): bigint {
    return this.s.SteamAPI_ISteamInventory_StartUpdateProperties(this.self);
  }

  /** Remove the property on the item */
  removeProperty(handle: bigint, nItemID: bigint, pchPropertyName: string): boolean {
    return this.s.SteamAPI_ISteamInventory_RemoveProperty(
      this.self,
      handle,
      nItemID,
      cstrArg(pchPropertyName),
    );
  }

  setPropertyString(
    handle: bigint,
    nItemID: bigint,
    pchPropertyName: string,
    pchPropertyValue: string,
  ): boolean {
    return this.s.SteamAPI_ISteamInventory_SetPropertyString(
      this.self,
      handle,
      nItemID,
      cstrArg(pchPropertyName),
      cstrArg(pchPropertyValue),
    );
  }

  setPropertyBool(
    handle: bigint,
    nItemID: bigint,
    pchPropertyName: string,
    bValue: boolean,
  ): boolean {
    return this.s.SteamAPI_ISteamInventory_SetPropertyBool(
      this.self,
      handle,
      nItemID,
      cstrArg(pchPropertyName),
      bValue,
    );
  }

  setPropertyInt64(
    handle: bigint,
    nItemID: bigint,
    pchPropertyName: string,
    nValue: bigint,
  ): boolean {
    return this.s.SteamAPI_ISteamInventory_SetPropertyInt64(
      this.self,
      handle,
      nItemID,
      cstrArg(pchPropertyName),
      nValue,
    );
  }

  setPropertyFloat(
    handle: bigint,
    nItemID: bigint,
    pchPropertyName: string,
    flValue: number,
  ): boolean {
    return this.s.SteamAPI_ISteamInventory_SetPropertyFloat(
      this.self,
      handle,
      nItemID,
      cstrArg(pchPropertyName),
      flValue,
    );
  }

  /** Submit the update request by handle */
  submitUpdateProperties(handle: bigint): { ok: boolean; pResultHandle: number } {
    const pResultHandle_buf = scalarOut("i32");
    const ok = this.s.SteamAPI_ISteamInventory_SubmitUpdateProperties(
      this.self,
      handle,
      pResultHandle_buf,
    );
    return { ok, pResultHandle: readScalar(pResultHandle_buf, "i32") as number };
  }

  inspectItem(pchItemToken: string): { ok: boolean; pResultHandle: number } {
    const pResultHandle_buf = scalarOut("i32");
    const ok = this.s.SteamAPI_ISteamInventory_InspectItem(
      this.self,
      pResultHandle_buf,
      cstrArg(pchItemToken),
    );
    return { ok, pResultHandle: readScalar(pResultHandle_buf, "i32") as number };
  }
}
