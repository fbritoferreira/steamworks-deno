// GENERATED by @steamworks/codegen from steam_api.json (SDK 1.65). Do not edit.

import type { CallResultHost } from "../../src/marshal.ts";
import { cstrArg, outString, readOutString, readScalar, scalarOut } from "../../src/marshal.ts";
import {
  decodeDurationControl_t,
  decodeEncryptedAppTicketResponse_t,
  decodeMarketEligibilityResponse_t,
  decodeStoreAuthURLResponse_t,
  DurationControl_t,
  encodeSteamNetworkingIdentity,
  EncryptedAppTicketResponse_t,
  MarketEligibilityResponse_t,
  SteamNetworkingIdentity,
  StoreAuthURLResponse_t,
} from "../structs.ts";
import type {
  EBeginAuthSessionResult,
  EDurationControlOnlineState,
  EUserHasLicenseForAppResult,
  EVoiceResult,
} from "../enums.ts";

export const ISteamUser_symbols = {
  SteamAPI_ISteamUser_GetHSteamUser: { parameters: ["pointer"], result: "i32" },
  SteamAPI_ISteamUser_BLoggedOn: { parameters: ["pointer"], result: "bool" },
  SteamAPI_ISteamUser_GetSteamID: { parameters: ["pointer"], result: "u64" },
  SteamAPI_ISteamUser_InitiateGameConnection_DEPRECATED: {
    parameters: ["pointer", "buffer", "i32", "u64", "u32", "u16", "bool"],
    result: "i32",
  },
  SteamAPI_ISteamUser_TerminateGameConnection_DEPRECATED: {
    parameters: ["pointer", "u32", "u16"],
    result: "void",
  },
  SteamAPI_ISteamUser_TrackAppUsageEvent: {
    parameters: ["pointer", "u64", "i32", "buffer"],
    result: "void",
  },
  SteamAPI_ISteamUser_GetUserDataFolder: {
    parameters: ["pointer", "buffer", "i32"],
    result: "bool",
  },
  SteamAPI_ISteamUser_StartVoiceRecording: { parameters: ["pointer"], result: "void" },
  SteamAPI_ISteamUser_StopVoiceRecording: { parameters: ["pointer"], result: "void" },
  SteamAPI_ISteamUser_GetAvailableVoice: {
    parameters: ["pointer", "buffer", "buffer", "u32"],
    result: "i32",
  },
  SteamAPI_ISteamUser_GetVoice: {
    parameters: [
      "pointer",
      "bool",
      "buffer",
      "u32",
      "buffer",
      "bool",
      "buffer",
      "u32",
      "buffer",
      "u32",
    ],
    result: "i32",
  },
  SteamAPI_ISteamUser_DecompressVoice: {
    parameters: ["pointer", "buffer", "u32", "buffer", "u32", "buffer", "u32"],
    result: "i32",
  },
  SteamAPI_ISteamUser_GetVoiceOptimalSampleRate: { parameters: ["pointer"], result: "u32" },
  SteamAPI_ISteamUser_GetAuthSessionTicket: {
    parameters: ["pointer", "buffer", "i32", "buffer", "buffer"],
    result: "u32",
  },
  SteamAPI_ISteamUser_GetAuthTicketForWebApi: { parameters: ["pointer", "buffer"], result: "u32" },
  SteamAPI_ISteamUser_BeginAuthSession: {
    parameters: ["pointer", "buffer", "i32", "u64"],
    result: "i32",
  },
  SteamAPI_ISteamUser_EndAuthSession: { parameters: ["pointer", "u64"], result: "void" },
  SteamAPI_ISteamUser_CancelAuthTicket: { parameters: ["pointer", "u32"], result: "void" },
  SteamAPI_ISteamUser_UserHasLicenseForApp: {
    parameters: ["pointer", "u64", "u32"],
    result: "i32",
  },
  SteamAPI_ISteamUser_BIsBehindNAT: { parameters: ["pointer"], result: "bool" },
  SteamAPI_ISteamUser_AdvertiseGame: {
    parameters: ["pointer", "u64", "u32", "u16"],
    result: "void",
  },
  SteamAPI_ISteamUser_RequestEncryptedAppTicket: {
    parameters: ["pointer", "buffer", "i32"],
    result: "u64",
  },
  SteamAPI_ISteamUser_GetEncryptedAppTicket: {
    parameters: ["pointer", "buffer", "i32", "buffer"],
    result: "bool",
  },
  SteamAPI_ISteamUser_GetGameBadgeLevel: { parameters: ["pointer", "i32", "bool"], result: "i32" },
  SteamAPI_ISteamUser_GetPlayerSteamLevel: { parameters: ["pointer"], result: "i32" },
  SteamAPI_ISteamUser_RequestStoreAuthURL: { parameters: ["pointer", "buffer"], result: "u64" },
  SteamAPI_ISteamUser_BIsPhoneVerified: { parameters: ["pointer"], result: "bool" },
  SteamAPI_ISteamUser_BIsTwoFactorEnabled: { parameters: ["pointer"], result: "bool" },
  SteamAPI_ISteamUser_BIsPhoneIdentifying: { parameters: ["pointer"], result: "bool" },
  SteamAPI_ISteamUser_BIsPhoneRequiringVerification: { parameters: ["pointer"], result: "bool" },
  SteamAPI_ISteamUser_GetMarketEligibility: { parameters: ["pointer"], result: "u64" },
  SteamAPI_ISteamUser_GetDurationControl: { parameters: ["pointer"], result: "u64" },
  SteamAPI_ISteamUser_BSetDurationControlOnlineState: {
    parameters: ["pointer", "i32"],
    result: "bool",
  },
  SteamAPI_SteamUser_v023: { parameters: [], result: "pointer", optional: true },
} as const satisfies Deno.ForeignLibraryInterface;

export class ISteamUser {
  static readonly accessor = "SteamAPI_SteamUser_v023";

  constructor(
    private readonly s: Deno.DynamicLibrary<typeof ISteamUser_symbols>["symbols"],
    private readonly self: Deno.PointerValue,
    private readonly host: CallResultHost,
  ) {}

  /**
   * returns the HSteamUser this interface represents
   * this is only used internally by the API, and by a few select interfaces that support multi-user
   */
  getHSteamUser(): number {
    return this.s.SteamAPI_ISteamUser_GetHSteamUser(this.self);
  }

  /**
   * returns true if the Steam client current has a live connection to the Steam servers.
   * If false, it means there is no active connection due to either a networking issue on the local machine, or the Steam server is down/busy.
   * The Steam client will automatically be trying to recreate the connection as often as possible.
   */
  bLoggedOn(): boolean {
    return this.s.SteamAPI_ISteamUser_BLoggedOn(this.self);
  }

  /**
   * returns the CSteamID of the account currently logged into the Steam client
   * a CSteamID is a unique identifier for an account, and used to differentiate users in all parts of the Steamworks API
   */
  getSteamID(): bigint {
    return this.s.SteamAPI_ISteamUser_GetSteamID(this.self);
  }

  /**
   * InitiateGameConnection() starts the state machine for authenticating the game client with the game server
   * It is the client portion of a three-way handshake between the client, the game server, and the steam servers
   * Parameters:
   * void *pAuthBlob - a pointer to empty memory that will be filled in with the authentication token.
   * int cbMaxAuthBlob - the number of bytes of allocated memory in pBlob. Should be at least 2048 bytes.
   * CSteamID steamIDGameServer - the steamID of the game server, received from the game server by the client
   * CGameID gameID - the ID of the current game. For games without mods, this is just CGameID( <appID> )
   * uint32 unIPServer, uint16 usPortServer - the IP address of the game server
   * bool bSecure - whether or not the client thinks that the game server is reporting itself as secure (i.e. VAC is running)
   * return value - returns the number of bytes written to pBlob. If the return is 0, then the buffer passed in was too small, and the call has failed
   * The contents of pBlob should then be sent to the game server, for it to use to complete the authentication process.
   * DEPRECATED!  This function will be removed from the SDK in an upcoming version.
   * Please migrate to BeginAuthSession and related functions.
   */
  initiateGameConnection_DEPRECATED(
    pAuthBlob: Uint8Array,
    cbMaxAuthBlob: number,
    steamIDGameServer: bigint,
    unIPServer: number,
    usPortServer: number,
    bSecure: boolean,
  ): number {
    return this.s.SteamAPI_ISteamUser_InitiateGameConnection_DEPRECATED(
      this.self,
      pAuthBlob,
      cbMaxAuthBlob,
      steamIDGameServer,
      unIPServer,
      usPortServer,
      bSecure,
    );
  }

  /**
   * notify of disconnect
   * needs to occur when the game client leaves the specified game server, needs to match with the InitiateGameConnection() call
   * DEPRECATED!  This function will be removed from the SDK in an upcoming version.
   * Please migrate to BeginAuthSession and related functions.
   */
  terminateGameConnection_DEPRECATED(unIPServer: number, usPortServer: number): void {
    this.s.SteamAPI_ISteamUser_TerminateGameConnection_DEPRECATED(
      this.self,
      unIPServer,
      usPortServer,
    );
  }

  /** used by only a few games to track usage events */
  trackAppUsageEvent(gameID: bigint, eAppUsageEvent: number, pchExtraInfo: string): void {
    this.s.SteamAPI_ISteamUser_TrackAppUsageEvent(
      this.self,
      gameID,
      eAppUsageEvent,
      cstrArg(pchExtraInfo),
    );
  }

  /**
   * get the local storage folder for current Steam account to write application data, e.g. save games, configs etc.
   * this will usually be something like "C:\Progam Files\Steam\userdata\<SteamID>\<AppID>\local"
   */
  getUserDataFolder(cubBuffer = 256): { ok: boolean; pchBuffer: string } {
    const pchBuffer_buf = outString(cubBuffer);
    const ok = this.s.SteamAPI_ISteamUser_GetUserDataFolder(this.self, pchBuffer_buf, cubBuffer);
    return { ok, pchBuffer: readOutString(pchBuffer_buf) };
  }

  /** Starts voice recording. Once started, use GetVoice() to get the data */
  startVoiceRecording(): void {
    this.s.SteamAPI_ISteamUser_StartVoiceRecording(this.self);
  }

  /**
   * Stops voice recording. Because people often release push-to-talk keys early, the system will keep recording for
   * a little bit after this function is called. GetVoice() should continue to be called until it returns
   * k_eVoiceResultNotRecording
   */
  stopVoiceRecording(): void {
    this.s.SteamAPI_ISteamUser_StopVoiceRecording(this.self);
  }

  /**
   * Determine the size of captured audio data that is available from GetVoice.
   * Most applications will only use compressed data and should ignore the other
   * parameters, which exist primarily for backwards compatibility. See comments
   * below for further explanation of "uncompressed" data.
   */
  getAvailableVoice(
    nUncompressedVoiceDesiredSampleRate_Deprecated: number,
  ): { result: EVoiceResult; pcbCompressed: number; pcbUncompressed_Deprecated: number } {
    const pcbCompressed_buf = scalarOut("u32");
    const pcbUncompressed_Deprecated_buf = scalarOut("u32");
    const result = this.s.SteamAPI_ISteamUser_GetAvailableVoice(
      this.self,
      pcbCompressed_buf,
      pcbUncompressed_Deprecated_buf,
      nUncompressedVoiceDesiredSampleRate_Deprecated,
    ) as EVoiceResult;
    return {
      result,
      pcbCompressed: readScalar(pcbCompressed_buf, "u32") as number,
      pcbUncompressed_Deprecated: readScalar(pcbUncompressed_Deprecated_buf, "u32") as number,
    };
  }

  /**
   * Read captured audio data from the microphone buffer. This should be called
   * at least once per frame, and preferably every few milliseconds, to keep the
   * microphone input delay as low as possible. Most applications will only use
   * compressed data and should pass NULL/zero for the "uncompressed" parameters.
   * Compressed data can be transmitted by your application and decoded into raw
   * using the DecompressVoice function below.
   */
  getVoice(
    bWantCompressed: boolean,
    pDestBuffer: Uint8Array,
    cbDestBufferSize: number,
    bWantUncompressed_Deprecated: boolean,
    pUncompressedDestBuffer_Deprecated: Uint8Array,
    cbUncompressedDestBufferSize_Deprecated: number,
    nUncompressedVoiceDesiredSampleRate_Deprecated: number,
  ): { result: EVoiceResult; nBytesWritten: number; nUncompressBytesWritten_Deprecated: number } {
    const nBytesWritten_buf = scalarOut("u32");
    const nUncompressBytesWritten_Deprecated_buf = scalarOut("u32");
    const result = this.s.SteamAPI_ISteamUser_GetVoice(
      this.self,
      bWantCompressed,
      pDestBuffer,
      cbDestBufferSize,
      nBytesWritten_buf,
      bWantUncompressed_Deprecated,
      pUncompressedDestBuffer_Deprecated,
      cbUncompressedDestBufferSize_Deprecated,
      nUncompressBytesWritten_Deprecated_buf,
      nUncompressedVoiceDesiredSampleRate_Deprecated,
    ) as EVoiceResult;
    return {
      result,
      nBytesWritten: readScalar(nBytesWritten_buf, "u32") as number,
      nUncompressBytesWritten_Deprecated: readScalar(
        nUncompressBytesWritten_Deprecated_buf,
        "u32",
      ) as number,
    };
  }

  /**
   * Decodes the compressed voice data returned by GetVoice. The output data is
   * raw single-channel 16-bit PCM audio. The decoder supports any sample rate
   * from 11025 to 48000; see GetVoiceOptimalSampleRate() below for details.
   * If the output buffer is not large enough, then *nBytesWritten will be set
   * to the required buffer size, and k_EVoiceResultBufferTooSmall is returned.
   * It is suggested to start with a 20kb buffer and reallocate as necessary.
   */
  decompressVoice(
    pCompressed: Uint8Array,
    cbCompressed: number,
    pDestBuffer: Uint8Array,
    cbDestBufferSize: number,
    nDesiredSampleRate: number,
  ): { result: EVoiceResult; nBytesWritten: number } {
    const nBytesWritten_buf = scalarOut("u32");
    const result = this.s.SteamAPI_ISteamUser_DecompressVoice(
      this.self,
      pCompressed,
      cbCompressed,
      pDestBuffer,
      cbDestBufferSize,
      nBytesWritten_buf,
      nDesiredSampleRate,
    ) as EVoiceResult;
    return { result, nBytesWritten: readScalar(nBytesWritten_buf, "u32") as number };
  }

  /**
   * This returns the native sample rate of the Steam voice decompressor; using
   * this sample rate for DecompressVoice will perform the least CPU processing.
   * However, the final audio quality will depend on how well the audio device
   * (and/or your application's audio output SDK) deals with lower sample rates.
   * You may find that you get the best audio output quality when you ignore
   * this function and use the native sample rate of your audio output device,
   * which is usually 48000 or 44100.
   */
  getVoiceOptimalSampleRate(): number {
    return this.s.SteamAPI_ISteamUser_GetVoiceOptimalSampleRate(this.self);
  }

  /**
   * Retrieve ticket to be sent to the entity who wishes to authenticate you.
   * pcbTicket retrieves the length of the actual ticket.
   * SteamNetworkingIdentity is an optional input parameter to hold the public IP address or SteamID of the entity you are connecting to
   * if an IP address is passed Steam will only allow the ticket to be used by an entity with that IP address
   * if a Steam ID is passed Steam will only allow the ticket to be used by that Steam ID
   * not to be used for "ISteamUserAuth\AuthenticateUserTicket" - it will fail
   */
  getAuthSessionTicket(
    pTicket: Uint8Array,
    cbMaxTicket: number,
    pSteamNetworkingIdentity: SteamNetworkingIdentity,
  ): { result: number; pcbTicket: number } {
    const pcbTicket_buf = scalarOut("u32");
    const result = this.s.SteamAPI_ISteamUser_GetAuthSessionTicket(
      this.self,
      pTicket,
      cbMaxTicket,
      pcbTicket_buf,
      encodeSteamNetworkingIdentity(pSteamNetworkingIdentity),
    );
    return { result, pcbTicket: readScalar(pcbTicket_buf, "u32") as number };
  }

  /**
   * Request a ticket which will be used for webapi "ISteamUserAuth\AuthenticateUserTicket"
   * pchIdentity is an optional input parameter to identify the service the ticket will be sent to
   * the ticket will be returned in callback GetTicketForWebApiResponse_t
   */
  getAuthTicketForWebApi(pchIdentity: string): number {
    return this.s.SteamAPI_ISteamUser_GetAuthTicketForWebApi(this.self, cstrArg(pchIdentity));
  }

  /**
   * Authenticate ticket from entity steamID to be sure it is valid and isnt reused
   * Registers for callbacks if the entity goes offline or cancels the ticket ( see ValidateAuthTicketResponse_t callback and EAuthSessionResponse )
   */
  beginAuthSession(
    pAuthTicket: Uint8Array,
    cbAuthTicket: number,
    steamID: bigint,
  ): EBeginAuthSessionResult {
    return this.s.SteamAPI_ISteamUser_BeginAuthSession(
      this.self,
      pAuthTicket,
      cbAuthTicket,
      steamID,
    ) as EBeginAuthSessionResult;
  }

  /** Stop tracking started by BeginAuthSession - called when no longer playing game with this entity */
  endAuthSession(steamID: bigint): void {
    this.s.SteamAPI_ISteamUser_EndAuthSession(this.self, steamID);
  }

  /** Cancel auth ticket from GetAuthSessionTicket, called when no longer playing game with the entity you gave the ticket to */
  cancelAuthTicket(hAuthTicket: number): void {
    this.s.SteamAPI_ISteamUser_CancelAuthTicket(this.self, hAuthTicket);
  }

  /**
   * After receiving a user's authentication data, and passing it to BeginAuthSession, use this function
   * to determine if the user owns downloadable content specified by the provided AppID.
   */
  userHasLicenseForApp(steamID: bigint, appID: number): EUserHasLicenseForAppResult {
    return this.s.SteamAPI_ISteamUser_UserHasLicenseForApp(
      this.self,
      steamID,
      appID,
    ) as EUserHasLicenseForAppResult;
  }

  /**
   * returns true if this users looks like they are behind a NAT device. Only valid once the user has connected to steam
   * (i.e a SteamServersConnected_t has been issued) and may not catch all forms of NAT.
   */
  bIsBehindNAT(): boolean {
    return this.s.SteamAPI_ISteamUser_BIsBehindNAT(this.self);
  }

  /**
   * set data to be replicated to friends so that they can join your game
   * CSteamID steamIDGameServer - the steamID of the game server, received from the game server by the client
   * uint32 unIPServer, uint16 usPortServer - the IP address of the game server
   */
  advertiseGame(steamIDGameServer: bigint, unIPServer: number, usPortServer: number): void {
    this.s.SteamAPI_ISteamUser_AdvertiseGame(
      this.self,
      steamIDGameServer,
      unIPServer,
      usPortServer,
    );
  }

  /**
   * Requests a ticket encrypted with an app specific shared key
   * pDataToInclude, cbDataToInclude will be encrypted into the ticket
   * ( This is asynchronous, you must wait for the ticket to be completed by the server )
   */
  requestEncryptedAppTicket(
    pDataToInclude: Uint8Array,
    cbDataToInclude: number,
  ): Promise<EncryptedAppTicketResponse_t> {
    const call = this.s.SteamAPI_ISteamUser_RequestEncryptedAppTicket(
      this.self,
      pDataToInclude,
      cbDataToInclude,
    );
    return this.host.callResult(call, 154, decodeEncryptedAppTicketResponse_t);
  }

  /**
   * Retrieves a finished ticket.
   * If no ticket is available, or your buffer is too small, returns false.
   * Upon exit, *pcbTicket will be either the size of the ticket copied into your buffer
   * (if true was returned), or the size needed (if false was returned).  To determine the
   * proper size of the ticket, you can pass pTicket=NULL and cbMaxTicket=0; if a ticket
   * is available, *pcbTicket will contain the size needed, otherwise it will be zero.
   */
  getEncryptedAppTicket(
    pTicket: Uint8Array,
    cbMaxTicket: number,
  ): { ok: boolean; pcbTicket: number } {
    const pcbTicket_buf = scalarOut("u32");
    const ok = this.s.SteamAPI_ISteamUser_GetEncryptedAppTicket(
      this.self,
      pTicket,
      cbMaxTicket,
      pcbTicket_buf,
    );
    return { ok, pcbTicket: readScalar(pcbTicket_buf, "u32") as number };
  }

  /**
   * Trading Card badges data access
   * if you only have one set of cards, the series will be 1
   * the user has can have two different badges for a series; the regular (max level 5) and the foil (max level 1)
   */
  getGameBadgeLevel(nSeries: number, bFoil: boolean): number {
    return this.s.SteamAPI_ISteamUser_GetGameBadgeLevel(this.self, nSeries, bFoil);
  }

  /** gets the Steam Level of the user, as shown on their profile */
  getPlayerSteamLevel(): number {
    return this.s.SteamAPI_ISteamUser_GetPlayerSteamLevel(this.self);
  }

  /**
   * Requests a URL which authenticates an in-game browser for store check-out,
   * and then redirects to the specified URL. As long as the in-game browser
   * accepts and handles session cookies, Steam microtransaction checkout pages
   * will automatically recognize the user instead of presenting a login page.
   * The result of this API call will be a StoreAuthURLResponse_t callback.
   * NOTE: The URL has a very short lifetime to prevent history-snooping attacks,
   * so you should only call this API when you are about to launch the browser,
   * or else immediately navigate to the result URL using a hidden browser window.
   * NOTE 2: The resulting authorization cookie has an expiration time of one day,
   * so it would be a good idea to request and visit a new auth URL every 12 hours.
   */
  requestStoreAuthURL(pchRedirectURL: string): Promise<StoreAuthURLResponse_t> {
    const call = this.s.SteamAPI_ISteamUser_RequestStoreAuthURL(this.self, cstrArg(pchRedirectURL));
    return this.host.callResult(call, 165, decodeStoreAuthURLResponse_t);
  }

  /** gets whether the users phone number is verified */
  bIsPhoneVerified(): boolean {
    return this.s.SteamAPI_ISteamUser_BIsPhoneVerified(this.self);
  }

  /** gets whether the user has two factor enabled on their account */
  bIsTwoFactorEnabled(): boolean {
    return this.s.SteamAPI_ISteamUser_BIsTwoFactorEnabled(this.self);
  }

  /** gets whether the users phone number is identifying */
  bIsPhoneIdentifying(): boolean {
    return this.s.SteamAPI_ISteamUser_BIsPhoneIdentifying(this.self);
  }

  /** gets whether the users phone number is awaiting (re)verification */
  bIsPhoneRequiringVerification(): boolean {
    return this.s.SteamAPI_ISteamUser_BIsPhoneRequiringVerification(this.self);
  }

  getMarketEligibility(): Promise<MarketEligibilityResponse_t> {
    const call = this.s.SteamAPI_ISteamUser_GetMarketEligibility(this.self);
    return this.host.callResult(call, 166, decodeMarketEligibilityResponse_t);
  }

  /** Retrieves anti indulgence / duration control for current user */
  getDurationControl(): Promise<DurationControl_t> {
    const call = this.s.SteamAPI_ISteamUser_GetDurationControl(this.self);
    return this.host.callResult(call, 167, decodeDurationControl_t);
  }

  /**
   * Advise steam china duration control system about the online state of the game.
   * This will prevent offline gameplay time from counting against a user's
   * playtime limits.
   */
  bSetDurationControlOnlineState(eNewState: EDurationControlOnlineState): boolean {
    return this.s.SteamAPI_ISteamUser_BSetDurationControlOnlineState(this.self, eNewState);
  }
}
