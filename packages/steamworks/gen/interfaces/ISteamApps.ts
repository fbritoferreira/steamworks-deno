// GENERATED by @steamworks/codegen from steam_api.json (SDK 1.65). Do not edit.

import { readCString } from "../../src/cstring.ts";
import type { CallResultHost } from "../../src/marshal.ts";
import {
  arrayOut,
  cstrArg,
  outString,
  readOutString,
  readScalar,
  readScalarArray,
  scalarOut,
} from "../../src/marshal.ts";
import { decodeFileDetailsResult_t, FileDetailsResult_t } from "../structs.ts";
import type { EGamePerformanceSetting } from "../enums.ts";

/**
 * Deno FFI symbol table for `ISteamApps`: every flat method, plus the versioned
 * accessor Steam uses to hand out the interface.
 */
export const ISteamApps_symbols = {
  SteamAPI_ISteamApps_BIsSubscribed: { parameters: ["pointer"], result: "bool" },
  SteamAPI_ISteamApps_BIsLowViolence: { parameters: ["pointer"], result: "bool" },
  SteamAPI_ISteamApps_BIsCybercafe: { parameters: ["pointer"], result: "bool" },
  SteamAPI_ISteamApps_BIsVACBanned: { parameters: ["pointer"], result: "bool" },
  SteamAPI_ISteamApps_GetCurrentGameLanguage: { parameters: ["pointer"], result: "pointer" },
  SteamAPI_ISteamApps_GetAvailableGameLanguages: { parameters: ["pointer"], result: "pointer" },
  SteamAPI_ISteamApps_BIsSubscribedApp: { parameters: ["pointer", "u32"], result: "bool" },
  SteamAPI_ISteamApps_BIsDlcInstalled: { parameters: ["pointer", "u32"], result: "bool" },
  SteamAPI_ISteamApps_GetEarliestPurchaseUnixTime: {
    parameters: ["pointer", "u32"],
    result: "u32",
  },
  SteamAPI_ISteamApps_BIsSubscribedFromFreeWeekend: { parameters: ["pointer"], result: "bool" },
  SteamAPI_ISteamApps_GetDLCCount: { parameters: ["pointer"], result: "i32" },
  SteamAPI_ISteamApps_BGetDLCDataByIndex: {
    parameters: ["pointer", "i32", "buffer", "buffer", "buffer", "i32"],
    result: "bool",
  },
  SteamAPI_ISteamApps_InstallDLC: { parameters: ["pointer", "u32"], result: "void" },
  SteamAPI_ISteamApps_UninstallDLC: { parameters: ["pointer", "u32"], result: "void" },
  SteamAPI_ISteamApps_RequestAppProofOfPurchaseKey: {
    parameters: ["pointer", "u32"],
    result: "void",
  },
  SteamAPI_ISteamApps_GetCurrentBetaName: {
    parameters: ["pointer", "buffer", "i32"],
    result: "bool",
  },
  SteamAPI_ISteamApps_MarkContentCorrupt: { parameters: ["pointer", "bool"], result: "bool" },
  SteamAPI_ISteamApps_GetInstalledDepots: {
    parameters: ["pointer", "u32", "buffer", "u32"],
    result: "u32",
  },
  SteamAPI_ISteamApps_GetAppInstallDir: {
    parameters: ["pointer", "u32", "buffer", "u32"],
    result: "u32",
  },
  SteamAPI_ISteamApps_BIsAppInstalled: { parameters: ["pointer", "u32"], result: "bool" },
  SteamAPI_ISteamApps_GetAppOwner: { parameters: ["pointer"], result: "u64" },
  SteamAPI_ISteamApps_GetLaunchQueryParam: { parameters: ["pointer", "buffer"], result: "pointer" },
  SteamAPI_ISteamApps_GetDlcDownloadProgress: {
    parameters: ["pointer", "u32", "buffer", "buffer"],
    result: "bool",
  },
  SteamAPI_ISteamApps_GetAppBuildId: { parameters: ["pointer"], result: "i32" },
  SteamAPI_ISteamApps_RequestAllProofOfPurchaseKeys: { parameters: ["pointer"], result: "void" },
  SteamAPI_ISteamApps_GetFileDetails: { parameters: ["pointer", "buffer"], result: "u64" },
  SteamAPI_ISteamApps_GetLaunchCommandLine: {
    parameters: ["pointer", "buffer", "i32"],
    result: "i32",
  },
  SteamAPI_ISteamApps_BIsSubscribedFromFamilySharing: { parameters: ["pointer"], result: "bool" },
  SteamAPI_ISteamApps_BIsTimedTrial: {
    parameters: ["pointer", "buffer", "buffer"],
    result: "bool",
  },
  SteamAPI_ISteamApps_SetDlcContext: { parameters: ["pointer", "u32"], result: "bool" },
  SteamAPI_ISteamApps_GetNumBetas: { parameters: ["pointer", "buffer", "buffer"], result: "i32" },
  SteamAPI_ISteamApps_GetBetaInfo: {
    parameters: ["pointer", "i32", "buffer", "buffer", "buffer", "i32", "buffer", "i32", "buffer"],
    result: "bool",
  },
  SteamAPI_ISteamApps_SetActiveBeta: { parameters: ["pointer", "buffer"], result: "bool" },
  SteamAPI_ISteamApps_SetGamePerformanceSetting: { parameters: ["pointer", "i32"], result: "void" },
  SteamAPI_ISteamApps_SetGameRenderResolution: {
    parameters: ["pointer", "u32", "u32"],
    result: "void",
  },
  SteamAPI_SteamApps_v009: { parameters: [], result: "pointer", optional: true },
} as const satisfies Deno.ForeignLibraryInterface;

/**
 * Steam's `ISteamApps` interface. Reach it from `SteamClient`; the constructor is
 * for the client to call.
 */
export class ISteamApps {
  /** The versioned export Steam uses to hand out this interface, for SDK 1.65. */
  static readonly accessor = "SteamAPI_SteamApps_v009";

  constructor(
    private readonly s: Deno.DynamicLibrary<typeof ISteamApps_symbols>["symbols"],
    private readonly self: Deno.PointerValue,
    private readonly host: CallResultHost,
  ) {}

  bIsSubscribed(): boolean {
    return this.s.SteamAPI_ISteamApps_BIsSubscribed(this.self);
  }

  bIsLowViolence(): boolean {
    return this.s.SteamAPI_ISteamApps_BIsLowViolence(this.self);
  }

  bIsCybercafe(): boolean {
    return this.s.SteamAPI_ISteamApps_BIsCybercafe(this.self);
  }

  bIsVACBanned(): boolean {
    return this.s.SteamAPI_ISteamApps_BIsVACBanned(this.self);
  }

  getCurrentGameLanguage(): string {
    return readCString(this.s.SteamAPI_ISteamApps_GetCurrentGameLanguage(this.self));
  }

  getAvailableGameLanguages(): string {
    return readCString(this.s.SteamAPI_ISteamApps_GetAvailableGameLanguages(this.self));
  }

  /** only use this member if you need to check ownership of another game related to yours, a demo for example */
  bIsSubscribedApp(appID: number): boolean {
    return this.s.SteamAPI_ISteamApps_BIsSubscribedApp(this.self, appID);
  }

  /** Takes AppID of DLC and checks if the user owns the DLC & if the DLC is installed */
  bIsDlcInstalled(appID: number): boolean {
    return this.s.SteamAPI_ISteamApps_BIsDlcInstalled(this.self, appID);
  }

  /** returns the Unix time of the purchase of the app */
  getEarliestPurchaseUnixTime(nAppID: number): number {
    return this.s.SteamAPI_ISteamApps_GetEarliestPurchaseUnixTime(this.self, nAppID);
  }

  /**
   * Checks if the user is subscribed to the current app through a free weekend
   * This function will return false for users who have a retail or other type of license
   * Before using, please ask your Valve technical contact how to package and secure your free weekend
   */
  bIsSubscribedFromFreeWeekend(): boolean {
    return this.s.SteamAPI_ISteamApps_BIsSubscribedFromFreeWeekend(this.self);
  }

  /** Returns the number of DLC pieces for the running app */
  getDLCCount(): number {
    return this.s.SteamAPI_ISteamApps_GetDLCCount(this.self);
  }

  /** Returns metadata for DLC by index, of range [0, GetDLCCount()] */
  bGetDLCDataByIndex(
    iDLC: number,
    cchNameBufferSize = 256,
  ): { ok: boolean; pAppID: number; pbAvailable: boolean; pchName: string } {
    const pAppID_buf = scalarOut("u32");
    const pbAvailable_buf = scalarOut("bool");
    const pchName_buf = outString(cchNameBufferSize);
    const ok = this.s.SteamAPI_ISteamApps_BGetDLCDataByIndex(
      this.self,
      iDLC,
      pAppID_buf,
      pbAvailable_buf,
      pchName_buf,
      cchNameBufferSize,
    );
    return {
      ok,
      pAppID: readScalar(pAppID_buf, "u32") as number,
      pbAvailable: readScalar(pbAvailable_buf, "bool") as boolean,
      pchName: readOutString(pchName_buf),
    };
  }

  /** Install/Uninstall control for optional DLC */
  installDLC(nAppID: number): void {
    this.s.SteamAPI_ISteamApps_InstallDLC(this.self, nAppID);
  }

  uninstallDLC(nAppID: number): void {
    this.s.SteamAPI_ISteamApps_UninstallDLC(this.self, nAppID);
  }

  /**
   * Request legacy cd-key for yourself or owned DLC. If you are interested in this
   * data then make sure you provide us with a list of valid keys to be distributed
   * to users when they purchase the game, before the game ships.
   * You'll receive an AppProofOfPurchaseKeyResponse_t callback when
   * the key is available (which may be immediately).
   */
  requestAppProofOfPurchaseKey(nAppID: number): void {
    this.s.SteamAPI_ISteamApps_RequestAppProofOfPurchaseKey(this.self, nAppID);
  }

  getCurrentBetaName(cchNameBufferSize = 256): { ok: boolean; pchName: string } {
    const pchName_buf = outString(cchNameBufferSize);
    const ok = this.s.SteamAPI_ISteamApps_GetCurrentBetaName(
      this.self,
      pchName_buf,
      cchNameBufferSize,
    );
    return { ok, pchName: readOutString(pchName_buf) };
  }

  markContentCorrupt(bMissingFilesOnly: boolean): boolean {
    return this.s.SteamAPI_ISteamApps_MarkContentCorrupt(this.self, bMissingFilesOnly);
  }

  getInstalledDepots(appID: number, cMaxDepots: number): number[] {
    const pvecDepots_buf = arrayOut("u32", cMaxDepots);
    const n = this.s.SteamAPI_ISteamApps_GetInstalledDepots(
      this.self,
      appID,
      pvecDepots_buf,
      cMaxDepots,
    );
    return readScalarArray(pvecDepots_buf, "u32", n) as number[];
  }

  /** returns current app install folder for AppID, returns folder name length */
  getAppInstallDir(
    appID: number,
    cchFolderBufferSize = 256,
  ): { result: number; pchFolder: string } {
    const pchFolder_buf = outString(cchFolderBufferSize);
    const result = this.s.SteamAPI_ISteamApps_GetAppInstallDir(
      this.self,
      appID,
      pchFolder_buf,
      cchFolderBufferSize,
    );
    return { result, pchFolder: readOutString(pchFolder_buf) };
  }

  bIsAppInstalled(appID: number): boolean {
    return this.s.SteamAPI_ISteamApps_BIsAppInstalled(this.self, appID);
  }

  /**
   * returns the SteamID of the original owner. If this CSteamID is different from ISteamUser::GetSteamID(),
   * the user has a temporary license borrowed via Family Sharing
   */
  getAppOwner(): bigint {
    return this.s.SteamAPI_ISteamApps_GetAppOwner(this.self);
  }

  /**
   * Returns the associated launch param if the game is run via steam://run/<appid>//?param1=value1&param2=value2&param3=value3 etc.
   * Parameter names starting with the character '@' are reserved for internal use and will always return and empty string.
   * Parameter names starting with an underscore '_' are reserved for steam features -- they can be queried by the game,
   * but it is advised that you not param names beginning with an underscore for your own features.
   * Check for new launch parameters on callback NewUrlLaunchParameters_t
   */
  getLaunchQueryParam(pchKey: string): string {
    return readCString(this.s.SteamAPI_ISteamApps_GetLaunchQueryParam(this.self, cstrArg(pchKey)));
  }

  /** get download progress for optional DLC */
  getDlcDownloadProgress(
    nAppID: number,
  ): { ok: boolean; punBytesDownloaded: bigint; punBytesTotal: bigint } {
    const punBytesDownloaded_buf = scalarOut("u64");
    const punBytesTotal_buf = scalarOut("u64");
    const ok = this.s.SteamAPI_ISteamApps_GetDlcDownloadProgress(
      this.self,
      nAppID,
      punBytesDownloaded_buf,
      punBytesTotal_buf,
    );
    return {
      ok,
      punBytesDownloaded: readScalar(punBytesDownloaded_buf, "u64") as bigint,
      punBytesTotal: readScalar(punBytesTotal_buf, "u64") as bigint,
    };
  }

  /** return the buildid of this app, may change at any time based on backend updates to the game */
  getAppBuildId(): number {
    return this.s.SteamAPI_ISteamApps_GetAppBuildId(this.self);
  }

  /**
   * Request all proof of purchase keys for the calling appid and associated DLC.
   * A series of AppProofOfPurchaseKeyResponse_t callbacks will be sent with
   * appropriate appid values, ending with a final callback where the m_nAppId
   * member is k_uAppIdInvalid (zero).
   */
  requestAllProofOfPurchaseKeys(): void {
    this.s.SteamAPI_ISteamApps_RequestAllProofOfPurchaseKeys(this.self);
  }

  getFileDetails(pszFileName: string): Promise<FileDetailsResult_t> {
    const call = this.s.SteamAPI_ISteamApps_GetFileDetails(this.self, cstrArg(pszFileName));
    return this.host.callResult(call, 1023, decodeFileDetailsResult_t);
  }

  /**
   * Get command line if game was launched via Steam URL, e.g. steam://run/<appid>//<command line>/.
   * This method of passing a connect string (used when joining via rich presence, accepting an
   * invite, etc) is preferable to passing the connect string on the operating system command
   * line, which is a security risk.  In order for rich presence joins to go through this
   * path and not be placed on the OS command line, you must set a value in your app's
   * configuration on Steam.  Ask Valve for help with this.
   * If game was already running and launched again, the NewUrlLaunchParameters_t will be fired.
   */
  getLaunchCommandLine(cubCommandLine = 256): { result: number; pszCommandLine: string } {
    const pszCommandLine_buf = outString(cubCommandLine);
    const result = this.s.SteamAPI_ISteamApps_GetLaunchCommandLine(
      this.self,
      pszCommandLine_buf,
      cubCommandLine,
    );
    return { result, pszCommandLine: readOutString(pszCommandLine_buf) };
  }

  /** Check if user borrowed this game via Family Sharing, If true, call GetAppOwner() to get the lender SteamID */
  bIsSubscribedFromFamilySharing(): boolean {
    return this.s.SteamAPI_ISteamApps_BIsSubscribedFromFamilySharing(this.self);
  }

  /** check if game is a timed trial with limited playtime */
  bIsTimedTrial(): { ok: boolean; punSecondsAllowed: number; punSecondsPlayed: number } {
    const punSecondsAllowed_buf = scalarOut("u32");
    const punSecondsPlayed_buf = scalarOut("u32");
    const ok = this.s.SteamAPI_ISteamApps_BIsTimedTrial(
      this.self,
      punSecondsAllowed_buf,
      punSecondsPlayed_buf,
    );
    return {
      ok,
      punSecondsAllowed: readScalar(punSecondsAllowed_buf, "u32") as number,
      punSecondsPlayed: readScalar(punSecondsPlayed_buf, "u32") as number,
    };
  }

  /** set current DLC AppID being played (or 0 if none). Allows Steam to track usage of major DLC extensions */
  setDlcContext(nAppID: number): boolean {
    return this.s.SteamAPI_ISteamApps_SetDlcContext(this.self, nAppID);
  }

  /** returns total number of known app branches (including default "public" branch ). nAvailable is number of available betas */
  getNumBetas(): { result: number; pnAvailable: number; pnPrivate: number } {
    const pnAvailable_buf = scalarOut("i32");
    const pnPrivate_buf = scalarOut("i32");
    const result = this.s.SteamAPI_ISteamApps_GetNumBetas(
      this.self,
      pnAvailable_buf,
      pnPrivate_buf,
    );
    return {
      result,
      pnAvailable: readScalar(pnAvailable_buf, "i32") as number,
      pnPrivate: readScalar(pnPrivate_buf, "i32") as number,
    };
  }

  /** return beta branch details, name, description, current BuildID and state flags (EBetaBranchFlags) */
  getBetaInfo(
    iBetaIndex: number,
    cchBetaName = 256,
    cchDescription = 256,
  ): {
    ok: boolean;
    punFlags: number;
    punBuildID: number;
    pchBetaName: string;
    pchDescription: string;
    punLastUpdated: number;
  } {
    const punFlags_buf = scalarOut("u32");
    const punBuildID_buf = scalarOut("u32");
    const pchBetaName_buf = outString(cchBetaName);
    const pchDescription_buf = outString(cchDescription);
    const punLastUpdated_buf = scalarOut("u32");
    const ok = this.s.SteamAPI_ISteamApps_GetBetaInfo(
      this.self,
      iBetaIndex,
      punFlags_buf,
      punBuildID_buf,
      pchBetaName_buf,
      cchBetaName,
      pchDescription_buf,
      cchDescription,
      punLastUpdated_buf,
    );
    return {
      ok,
      punFlags: readScalar(punFlags_buf, "u32") as number,
      punBuildID: readScalar(punBuildID_buf, "u32") as number,
      pchBetaName: readOutString(pchBetaName_buf),
      pchDescription: readOutString(pchDescription_buf),
      punLastUpdated: readScalar(punLastUpdated_buf, "u32") as number,
    };
  }

  /** select this beta branch for this app as active, might need the game to restart so Steam can update to that branch */
  setActiveBeta(pchBetaName: string): boolean {
    return this.s.SteamAPI_ISteamApps_SetActiveBeta(this.self, cstrArg(pchBetaName));
  }

  /** game performance settings */
  setGamePerformanceSetting(setting: EGamePerformanceSetting): void {
    this.s.SteamAPI_ISteamApps_SetGamePerformanceSetting(this.self, setting);
  }

  setGameRenderResolution(unWidth: number, unHeight: number): void {
    this.s.SteamAPI_ISteamApps_SetGameRenderResolution(this.self, unWidth, unHeight);
  }
}
