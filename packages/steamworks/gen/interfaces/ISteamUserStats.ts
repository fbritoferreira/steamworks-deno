// GENERATED by @steamworks/codegen from steam_api.json (SDK 1.65). Do not edit.

import { PACK } from "../../src/layout.ts";
import { readCString } from "../../src/cstring.ts";
import type { CallResultHost } from "../../src/marshal.ts";
import { cstrArg, readScalar, scalarOut, writeScalarArray } from "../../src/marshal.ts";
import {
  decodeGlobalAchievementPercentagesReady_t,
  decodeGlobalStatsReceived_t,
  decodeLeaderboardEntry_t,
  decodeLeaderboardFindResult_t,
  decodeLeaderboardScoresDownloaded_t,
  decodeLeaderboardScoreUploaded_t,
  decodeLeaderboardUGCSet_t,
  decodeNumberOfCurrentPlayers_t,
  decodeUserStatsReceived_t,
  GlobalAchievementPercentagesReady_t,
  GlobalStatsReceived_t,
  LeaderboardEntry_t,
  LeaderboardEntry_t_layout,
  LeaderboardFindResult_t,
  LeaderboardScoresDownloaded_t,
  LeaderboardScoreUploaded_t,
  LeaderboardUGCSet_t,
  NumberOfCurrentPlayers_t,
  UserStatsReceived_t,
} from "../structs.ts";
import type {
  ELeaderboardDataRequest,
  ELeaderboardDisplayType,
  ELeaderboardSortMethod,
  ELeaderboardUploadScoreMethod,
} from "../enums.ts";

/**
 * Deno FFI symbol table for `ISteamUserStats`: every flat method, plus the versioned
 * accessor Steam uses to hand out the interface.
 */
export const ISteamUserStats_symbols = {
  SteamAPI_ISteamUserStats_GetStatInt32: {
    parameters: ["pointer", "buffer", "buffer"],
    result: "bool",
  },
  SteamAPI_ISteamUserStats_GetStatFloat: {
    parameters: ["pointer", "buffer", "buffer"],
    result: "bool",
  },
  SteamAPI_ISteamUserStats_SetStatInt32: {
    parameters: ["pointer", "buffer", "i32"],
    result: "bool",
  },
  SteamAPI_ISteamUserStats_SetStatFloat: {
    parameters: ["pointer", "buffer", "f32"],
    result: "bool",
  },
  SteamAPI_ISteamUserStats_UpdateAvgRateStat: {
    parameters: ["pointer", "buffer", "f32", "f64"],
    result: "bool",
  },
  SteamAPI_ISteamUserStats_GetAchievement: {
    parameters: ["pointer", "buffer", "buffer"],
    result: "bool",
  },
  SteamAPI_ISteamUserStats_SetAchievement: { parameters: ["pointer", "buffer"], result: "bool" },
  SteamAPI_ISteamUserStats_ClearAchievement: { parameters: ["pointer", "buffer"], result: "bool" },
  SteamAPI_ISteamUserStats_GetAchievementAndUnlockTime: {
    parameters: ["pointer", "buffer", "buffer", "buffer"],
    result: "bool",
  },
  SteamAPI_ISteamUserStats_StoreStats: { parameters: ["pointer"], result: "bool" },
  SteamAPI_ISteamUserStats_GetAchievementIcon: { parameters: ["pointer", "buffer"], result: "i32" },
  SteamAPI_ISteamUserStats_GetAchievementDisplayAttribute: {
    parameters: ["pointer", "buffer", "buffer"],
    result: "pointer",
  },
  SteamAPI_ISteamUserStats_IndicateAchievementProgress: {
    parameters: ["pointer", "buffer", "u32", "u32"],
    result: "bool",
  },
  SteamAPI_ISteamUserStats_GetNumAchievements: { parameters: ["pointer"], result: "u32" },
  SteamAPI_ISteamUserStats_GetAchievementName: {
    parameters: ["pointer", "u32"],
    result: "pointer",
  },
  SteamAPI_ISteamUserStats_RequestUserStats: { parameters: ["pointer", "u64"], result: "u64" },
  SteamAPI_ISteamUserStats_GetUserStatInt32: {
    parameters: ["pointer", "u64", "buffer", "buffer"],
    result: "bool",
  },
  SteamAPI_ISteamUserStats_GetUserStatFloat: {
    parameters: ["pointer", "u64", "buffer", "buffer"],
    result: "bool",
  },
  SteamAPI_ISteamUserStats_GetUserAchievement: {
    parameters: ["pointer", "u64", "buffer", "buffer"],
    result: "bool",
  },
  SteamAPI_ISteamUserStats_GetUserAchievementAndUnlockTime: {
    parameters: ["pointer", "u64", "buffer", "buffer", "buffer"],
    result: "bool",
  },
  SteamAPI_ISteamUserStats_ResetAllStats: { parameters: ["pointer", "bool"], result: "bool" },
  SteamAPI_ISteamUserStats_FindOrCreateLeaderboard: {
    parameters: ["pointer", "buffer", "i32", "i32"],
    result: "u64",
  },
  SteamAPI_ISteamUserStats_FindLeaderboard: { parameters: ["pointer", "buffer"], result: "u64" },
  SteamAPI_ISteamUserStats_GetLeaderboardName: {
    parameters: ["pointer", "u64"],
    result: "pointer",
  },
  SteamAPI_ISteamUserStats_GetLeaderboardEntryCount: {
    parameters: ["pointer", "u64"],
    result: "i32",
  },
  SteamAPI_ISteamUserStats_GetLeaderboardSortMethod: {
    parameters: ["pointer", "u64"],
    result: "i32",
  },
  SteamAPI_ISteamUserStats_GetLeaderboardDisplayType: {
    parameters: ["pointer", "u64"],
    result: "i32",
  },
  SteamAPI_ISteamUserStats_DownloadLeaderboardEntries: {
    parameters: ["pointer", "u64", "i32", "i32", "i32"],
    result: "u64",
  },
  SteamAPI_ISteamUserStats_DownloadLeaderboardEntriesForUsers: {
    parameters: ["pointer", "u64", "buffer", "i32"],
    result: "u64",
  },
  SteamAPI_ISteamUserStats_GetDownloadedLeaderboardEntry: {
    parameters: ["pointer", "u64", "i32", "buffer", "buffer", "i32"],
    result: "bool",
  },
  SteamAPI_ISteamUserStats_UploadLeaderboardScore: {
    parameters: ["pointer", "u64", "i32", "i32", "pointer", "i32"],
    result: "u64",
  },
  SteamAPI_ISteamUserStats_AttachLeaderboardUGC: {
    parameters: ["pointer", "u64", "u64"],
    result: "u64",
  },
  SteamAPI_ISteamUserStats_GetNumberOfCurrentPlayers: { parameters: ["pointer"], result: "u64" },
  SteamAPI_ISteamUserStats_RequestGlobalAchievementPercentages: {
    parameters: ["pointer"],
    result: "u64",
  },
  SteamAPI_ISteamUserStats_GetMostAchievedAchievementInfo: {
    parameters: ["pointer", "buffer", "u32", "buffer", "buffer"],
    result: "i32",
  },
  SteamAPI_ISteamUserStats_GetNextMostAchievedAchievementInfo: {
    parameters: ["pointer", "i32", "buffer", "u32", "buffer", "buffer"],
    result: "i32",
  },
  SteamAPI_ISteamUserStats_GetAchievementAchievedPercent: {
    parameters: ["pointer", "buffer", "buffer"],
    result: "bool",
  },
  SteamAPI_ISteamUserStats_RequestGlobalStats: { parameters: ["pointer", "i32"], result: "u64" },
  SteamAPI_ISteamUserStats_GetGlobalStatInt64: {
    parameters: ["pointer", "buffer", "buffer"],
    result: "bool",
  },
  SteamAPI_ISteamUserStats_GetGlobalStatDouble: {
    parameters: ["pointer", "buffer", "buffer"],
    result: "bool",
  },
  SteamAPI_ISteamUserStats_GetGlobalStatHistoryInt64: {
    parameters: ["pointer", "buffer", "buffer", "u32"],
    result: "i32",
  },
  SteamAPI_ISteamUserStats_GetGlobalStatHistoryDouble: {
    parameters: ["pointer", "buffer", "buffer", "u32"],
    result: "i32",
  },
  SteamAPI_ISteamUserStats_GetAchievementProgressLimitsInt32: {
    parameters: ["pointer", "buffer", "buffer", "buffer"],
    result: "bool",
  },
  SteamAPI_ISteamUserStats_GetAchievementProgressLimitsFloat: {
    parameters: ["pointer", "buffer", "buffer", "buffer"],
    result: "bool",
  },
  SteamAPI_SteamUserStats_v013: { parameters: [], result: "pointer", optional: true },
} as const satisfies Deno.ForeignLibraryInterface;

/**
 * Steam's `ISteamUserStats` interface. Reach it from `SteamClient`; the constructor is
 * for the client to call.
 */
export class ISteamUserStats {
  /** The versioned export Steam uses to hand out this interface, for SDK 1.65. */
  static readonly accessor = "SteamAPI_SteamUserStats_v013";

  constructor(
    private readonly s: Deno.DynamicLibrary<typeof ISteamUserStats_symbols>["symbols"],
    private readonly self: Deno.PointerValue,
    private readonly host: CallResultHost,
  ) {}

  /** Data accessors */
  getStatInt32(pchName: string): { ok: boolean; pData: number } {
    const pData_buf = scalarOut("i32");
    const ok = this.s.SteamAPI_ISteamUserStats_GetStatInt32(this.self, cstrArg(pchName), pData_buf);
    return { ok, pData: readScalar(pData_buf, "i32") as number };
  }

  /** Data accessors */
  getStatFloat(pchName: string): { ok: boolean; pData: number } {
    const pData_buf = scalarOut("f32");
    const ok = this.s.SteamAPI_ISteamUserStats_GetStatFloat(this.self, cstrArg(pchName), pData_buf);
    return { ok, pData: readScalar(pData_buf, "f32") as number };
  }

  /** Set / update data */
  setStatInt32(pchName: string, nData: number): boolean {
    return this.s.SteamAPI_ISteamUserStats_SetStatInt32(this.self, cstrArg(pchName), nData);
  }

  /** Set / update data */
  setStatFloat(pchName: string, fData: number): boolean {
    return this.s.SteamAPI_ISteamUserStats_SetStatFloat(this.self, cstrArg(pchName), fData);
  }

  updateAvgRateStat(pchName: string, flCountThisSession: number, dSessionLength: number): boolean {
    return this.s.SteamAPI_ISteamUserStats_UpdateAvgRateStat(
      this.self,
      cstrArg(pchName),
      flCountThisSession,
      dSessionLength,
    );
  }

  /** Achievement flag accessors */
  getAchievement(pchName: string): { ok: boolean; pbAchieved: boolean } {
    const pbAchieved_buf = scalarOut("bool");
    const ok = this.s.SteamAPI_ISteamUserStats_GetAchievement(
      this.self,
      cstrArg(pchName),
      pbAchieved_buf,
    );
    return { ok, pbAchieved: readScalar(pbAchieved_buf, "bool") as boolean };
  }

  setAchievement(pchName: string): boolean {
    return this.s.SteamAPI_ISteamUserStats_SetAchievement(this.self, cstrArg(pchName));
  }

  clearAchievement(pchName: string): boolean {
    return this.s.SteamAPI_ISteamUserStats_ClearAchievement(this.self, cstrArg(pchName));
  }

  /**
   * Get the achievement status, and the time it was unlocked if unlocked.
   * If the return value is true, but the unlock time is zero, that means it was unlocked before Steam
   * began tracking achievement unlock times (December 2009). Time is seconds since January 1, 1970.
   */
  getAchievementAndUnlockTime(
    pchName: string,
  ): { ok: boolean; pbAchieved: boolean; punUnlockTime: number } {
    const pbAchieved_buf = scalarOut("bool");
    const punUnlockTime_buf = scalarOut("u32");
    const ok = this.s.SteamAPI_ISteamUserStats_GetAchievementAndUnlockTime(
      this.self,
      cstrArg(pchName),
      pbAchieved_buf,
      punUnlockTime_buf,
    );
    return {
      ok,
      pbAchieved: readScalar(pbAchieved_buf, "bool") as boolean,
      punUnlockTime: readScalar(punUnlockTime_buf, "u32") as number,
    };
  }

  /**
   * Store the current data on the server, will get a callback when set
   * And one callback for every new achievement
   * If the callback has a result of k_EResultInvalidParam, one or more stats
   * uploaded has been rejected, either because they broke constraints
   * or were out of date. In this case the server sends back updated values.
   * The stats should be re-iterated to keep in sync.
   */
  storeStats(): boolean {
    return this.s.SteamAPI_ISteamUserStats_StoreStats(this.self);
  }

  /**
   * Gets the icon of the achievement, which is a handle to be used in ISteamUtils::GetImageRGBA(), or 0 if none set.
   * A return value of 0 may indicate we are still fetching data, and you can wait for the UserAchievementIconFetched_t callback
   * which will notify you when the bits are ready. If the callback still returns zero, then there is no image set for the
   * specified achievement.
   */
  getAchievementIcon(pchName: string): number {
    return this.s.SteamAPI_ISteamUserStats_GetAchievementIcon(this.self, cstrArg(pchName));
  }

  /**
   * Get general attributes for an achievement. Accepts the following keys:
   * - "name" and "desc" for retrieving the localized achievement name and description (returned in UTF8)
   * - "hidden" for retrieving if an achievement is hidden (returns "0" when not hidden, "1" when hidden)
   */
  getAchievementDisplayAttribute(pchName: string, pchKey: string): string {
    return readCString(
      this.s.SteamAPI_ISteamUserStats_GetAchievementDisplayAttribute(
        this.self,
        cstrArg(pchName),
        cstrArg(pchKey),
      ),
    );
  }

  /**
   * Achievement progress - triggers an AchievementProgress callback, that is all.
   * Calling this w/ N out of N progress will NOT set the achievement, the game must still do that.
   */
  indicateAchievementProgress(
    pchName: string,
    nCurProgress: number,
    nMaxProgress: number,
  ): boolean {
    return this.s.SteamAPI_ISteamUserStats_IndicateAchievementProgress(
      this.self,
      cstrArg(pchName),
      nCurProgress,
      nMaxProgress,
    );
  }

  /**
   * Used for iterating achievements. In general games should not need these functions because they should have a
   * list of existing achievements compiled into them
   */
  getNumAchievements(): number {
    return this.s.SteamAPI_ISteamUserStats_GetNumAchievements(this.self);
  }

  /** Get achievement name iAchievement in [0,GetNumAchievements) */
  getAchievementName(iAchievement: number): string {
    return readCString(this.s.SteamAPI_ISteamUserStats_GetAchievementName(this.self, iAchievement));
  }

  /**
   * downloads stats for the user
   * returns a UserStatsReceived_t received when completed
   * if the other user has no stats, UserStatsReceived_t.m_eResult will be set to k_EResultFail
   * these stats won't be auto-updated; you'll need to call RequestUserStats() again to refresh any data
   */
  requestUserStats(steamIDUser: bigint): Promise<UserStatsReceived_t> {
    const call = this.s.SteamAPI_ISteamUserStats_RequestUserStats(this.self, steamIDUser);
    return this.host.callResult(call, 1101, decodeUserStatsReceived_t);
  }

  /** requests stat information for a user, usable after a successful call to RequestUserStats() */
  getUserStatInt32(steamIDUser: bigint, pchName: string): { ok: boolean; pData: number } {
    const pData_buf = scalarOut("i32");
    const ok = this.s.SteamAPI_ISteamUserStats_GetUserStatInt32(
      this.self,
      steamIDUser,
      cstrArg(pchName),
      pData_buf,
    );
    return { ok, pData: readScalar(pData_buf, "i32") as number };
  }

  /** requests stat information for a user, usable after a successful call to RequestUserStats() */
  getUserStatFloat(steamIDUser: bigint, pchName: string): { ok: boolean; pData: number } {
    const pData_buf = scalarOut("f32");
    const ok = this.s.SteamAPI_ISteamUserStats_GetUserStatFloat(
      this.self,
      steamIDUser,
      cstrArg(pchName),
      pData_buf,
    );
    return { ok, pData: readScalar(pData_buf, "f32") as number };
  }

  getUserAchievement(steamIDUser: bigint, pchName: string): { ok: boolean; pbAchieved: boolean } {
    const pbAchieved_buf = scalarOut("bool");
    const ok = this.s.SteamAPI_ISteamUserStats_GetUserAchievement(
      this.self,
      steamIDUser,
      cstrArg(pchName),
      pbAchieved_buf,
    );
    return { ok, pbAchieved: readScalar(pbAchieved_buf, "bool") as boolean };
  }

  /** See notes for GetAchievementAndUnlockTime above */
  getUserAchievementAndUnlockTime(
    steamIDUser: bigint,
    pchName: string,
  ): { ok: boolean; pbAchieved: boolean; punUnlockTime: number } {
    const pbAchieved_buf = scalarOut("bool");
    const punUnlockTime_buf = scalarOut("u32");
    const ok = this.s.SteamAPI_ISteamUserStats_GetUserAchievementAndUnlockTime(
      this.self,
      steamIDUser,
      cstrArg(pchName),
      pbAchieved_buf,
      punUnlockTime_buf,
    );
    return {
      ok,
      pbAchieved: readScalar(pbAchieved_buf, "bool") as boolean,
      punUnlockTime: readScalar(punUnlockTime_buf, "u32") as number,
    };
  }

  /** Reset stats */
  resetAllStats(bAchievementsToo: boolean): boolean {
    return this.s.SteamAPI_ISteamUserStats_ResetAllStats(this.self, bAchievementsToo);
  }

  /**
   * asks the Steam back-end for a leaderboard by name, and will create it if it's not yet
   * This call is asynchronous, with the result returned in LeaderboardFindResult_t
   */
  findOrCreateLeaderboard(
    pchLeaderboardName: string,
    eLeaderboardSortMethod: ELeaderboardSortMethod,
    eLeaderboardDisplayType: ELeaderboardDisplayType,
  ): Promise<LeaderboardFindResult_t> {
    const call = this.s.SteamAPI_ISteamUserStats_FindOrCreateLeaderboard(
      this.self,
      cstrArg(pchLeaderboardName),
      eLeaderboardSortMethod,
      eLeaderboardDisplayType,
    );
    return this.host.callResult(call, 1104, decodeLeaderboardFindResult_t);
  }

  /**
   * as above, but won't create the leaderboard if it's not found
   * This call is asynchronous, with the result returned in LeaderboardFindResult_t
   */
  findLeaderboard(pchLeaderboardName: string): Promise<LeaderboardFindResult_t> {
    const call = this.s.SteamAPI_ISteamUserStats_FindLeaderboard(
      this.self,
      cstrArg(pchLeaderboardName),
    );
    return this.host.callResult(call, 1104, decodeLeaderboardFindResult_t);
  }

  /** returns the name of a leaderboard */
  getLeaderboardName(hSteamLeaderboard: bigint): string {
    return readCString(
      this.s.SteamAPI_ISteamUserStats_GetLeaderboardName(this.self, hSteamLeaderboard),
    );
  }

  /** returns the total number of entries in a leaderboard, as of the last request */
  getLeaderboardEntryCount(hSteamLeaderboard: bigint): number {
    return this.s.SteamAPI_ISteamUserStats_GetLeaderboardEntryCount(this.self, hSteamLeaderboard);
  }

  /** returns the sort method of the leaderboard */
  getLeaderboardSortMethod(hSteamLeaderboard: bigint): ELeaderboardSortMethod {
    return this.s.SteamAPI_ISteamUserStats_GetLeaderboardSortMethod(
      this.self,
      hSteamLeaderboard,
    ) as ELeaderboardSortMethod;
  }

  /** returns the display type of the leaderboard */
  getLeaderboardDisplayType(hSteamLeaderboard: bigint): ELeaderboardDisplayType {
    return this.s.SteamAPI_ISteamUserStats_GetLeaderboardDisplayType(
      this.self,
      hSteamLeaderboard,
    ) as ELeaderboardDisplayType;
  }

  /**
   * Asks the Steam back-end for a set of rows in the leaderboard.
   * This call is asynchronous, with the result returned in LeaderboardScoresDownloaded_t
   * LeaderboardScoresDownloaded_t will contain a handle to pull the results from GetDownloadedLeaderboardEntries() (below)
   * You can ask for more entries than exist, and it will return as many as do exist.
   * k_ELeaderboardDataRequestGlobal requests rows in the leaderboard from the full table, with nRangeStart & nRangeEnd in the range [1, TotalEntries]
   * k_ELeaderboardDataRequestGlobalAroundUser requests rows around the current user, nRangeStart being negate
   * e.g. DownloadLeaderboardEntries( hLeaderboard, k_ELeaderboardDataRequestGlobalAroundUser, -3, 3 ) will return 7 rows, 3 before the user, 3 after
   * k_ELeaderboardDataRequestFriends requests all the rows for friends of the current user
   */
  downloadLeaderboardEntries(
    hSteamLeaderboard: bigint,
    eLeaderboardDataRequest: ELeaderboardDataRequest,
    nRangeStart: number,
    nRangeEnd: number,
  ): Promise<LeaderboardScoresDownloaded_t> {
    const call = this.s.SteamAPI_ISteamUserStats_DownloadLeaderboardEntries(
      this.self,
      hSteamLeaderboard,
      eLeaderboardDataRequest,
      nRangeStart,
      nRangeEnd,
    );
    return this.host.callResult(call, 1105, decodeLeaderboardScoresDownloaded_t);
  }

  /**
   * as above, but downloads leaderboard entries for an arbitrary set of users - ELeaderboardDataRequest is k_ELeaderboardDataRequestUsers
   * if a user doesn't have a leaderboard entry, they won't be included in the result
   * a max of 100 users can be downloaded at a time, with only one outstanding call at a time
   */
  downloadLeaderboardEntriesForUsers(
    hSteamLeaderboard: bigint,
    prgUsers: bigint[],
    cUsers: number,
  ): Promise<LeaderboardScoresDownloaded_t> {
    const call = this.s.SteamAPI_ISteamUserStats_DownloadLeaderboardEntriesForUsers(
      this.self,
      hSteamLeaderboard,
      writeScalarArray("u64", prgUsers),
      cUsers,
    );
    return this.host.callResult(call, 1105, decodeLeaderboardScoresDownloaded_t);
  }

  /**
   * Returns data about a single leaderboard entry
   * use a for loop from 0 to LeaderboardScoresDownloaded_t::m_cEntryCount to get all the downloaded entries
   * e.g.
   * void OnLeaderboardScoresDownloaded( LeaderboardScoresDownloaded_t *pLeaderboardScoresDownloaded )
   * {
   * for ( int index = 0; index < pLeaderboardScoresDownloaded->m_cEntryCount; index++ )
   * {
   * LeaderboardEntry_t leaderboardEntry;
   * int32 details[3];		// we know this is how many we've stored previously
   * GetDownloadedLeaderboardEntry( pLeaderboardScoresDownloaded->m_hSteamLeaderboardEntries, index, &leaderboardEntry, details, 3 );
   * assert( leaderboardEntry.m_cDetails == 3 );
   * ...
   * }
   * once you've accessed all the entries, the data will be free'd, and the SteamLeaderboardEntries_t handle will become invalid
   */
  getDownloadedLeaderboardEntry(
    hSteamLeaderboardEntries: bigint,
    index: number,
    cDetailsMax: number,
  ): LeaderboardEntry_t | null {
    const pLeaderboardEntry_buf = new Uint8Array(LeaderboardEntry_t_layout[PACK].size);
    const pDetails_buf = scalarOut("i32");
    if (
      !this.s.SteamAPI_ISteamUserStats_GetDownloadedLeaderboardEntry(
        this.self,
        hSteamLeaderboardEntries,
        index,
        pLeaderboardEntry_buf,
        pDetails_buf,
        cDetailsMax,
      )
    ) return null;
    return decodeLeaderboardEntry_t(pLeaderboardEntry_buf);
  }

  /**
   * Uploads a user score to the Steam back-end.
   * This call is asynchronous, with the result returned in LeaderboardScoreUploaded_t
   * Details are extra game-defined information regarding how the user got that score
   * pScoreDetails points to an array of int32's, cScoreDetailsCount is the number of int32's in the list
   */
  uploadLeaderboardScore(
    hSteamLeaderboard: bigint,
    eLeaderboardUploadScoreMethod: ELeaderboardUploadScoreMethod,
    nScore: number,
    pScoreDetails: Deno.PointerValue,
    cScoreDetailsCount: number,
  ): Promise<LeaderboardScoreUploaded_t> {
    const call = this.s.SteamAPI_ISteamUserStats_UploadLeaderboardScore(
      this.self,
      hSteamLeaderboard,
      eLeaderboardUploadScoreMethod,
      nScore,
      pScoreDetails,
      cScoreDetailsCount,
    );
    return this.host.callResult(call, 1106, decodeLeaderboardScoreUploaded_t);
  }

  /**
   * Attaches a piece of user generated content the user's entry on a leaderboard.
   * hContent is a handle to a piece of user generated content that was shared using ISteamUserRemoteStorage::FileShare().
   * This call is asynchronous, with the result returned in LeaderboardUGCSet_t.
   */
  attachLeaderboardUGC(hSteamLeaderboard: bigint, hUGC: bigint): Promise<LeaderboardUGCSet_t> {
    const call = this.s.SteamAPI_ISteamUserStats_AttachLeaderboardUGC(
      this.self,
      hSteamLeaderboard,
      hUGC,
    );
    return this.host.callResult(call, 1111, decodeLeaderboardUGCSet_t);
  }

  /**
   * Retrieves the number of players currently playing your game (online + offline)
   * This call is asynchronous, with the result returned in NumberOfCurrentPlayers_t
   */
  getNumberOfCurrentPlayers(): Promise<NumberOfCurrentPlayers_t> {
    const call = this.s.SteamAPI_ISteamUserStats_GetNumberOfCurrentPlayers(this.self);
    return this.host.callResult(call, 1107, decodeNumberOfCurrentPlayers_t);
  }

  /**
   * Requests that Steam fetch data on the percentage of players who have received each achievement
   * for the game globally.
   * This call is asynchronous, with the result returned in GlobalAchievementPercentagesReady_t.
   */
  requestGlobalAchievementPercentages(): Promise<GlobalAchievementPercentagesReady_t> {
    const call = this.s.SteamAPI_ISteamUserStats_RequestGlobalAchievementPercentages(this.self);
    return this.host.callResult(call, 1110, decodeGlobalAchievementPercentagesReady_t);
  }

  /**
   * Get the info on the most achieved achievement for the game, returns an iterator index you can use to fetch
   * the next most achieved afterwards.  Will return -1 if there is no data on achievement
   * percentages (ie, you haven't called RequestGlobalAchievementPercentages and waited on the callback).
   */
  getMostAchievedAchievementInfo(
    pchName: string,
    unNameBufLen: number,
  ): { result: number; pflPercent: number; pbAchieved: boolean } {
    const pflPercent_buf = scalarOut("f32");
    const pbAchieved_buf = scalarOut("bool");
    const result = this.s.SteamAPI_ISteamUserStats_GetMostAchievedAchievementInfo(
      this.self,
      cstrArg(pchName),
      unNameBufLen,
      pflPercent_buf,
      pbAchieved_buf,
    );
    return {
      result,
      pflPercent: readScalar(pflPercent_buf, "f32") as number,
      pbAchieved: readScalar(pbAchieved_buf, "bool") as boolean,
    };
  }

  /**
   * Get the info on the next most achieved achievement for the game. Call this after GetMostAchievedAchievementInfo or another
   * GetNextMostAchievedAchievementInfo call passing the iterator from the previous call. Returns -1 after the last
   * achievement has been iterated.
   */
  getNextMostAchievedAchievementInfo(
    iIteratorPrevious: number,
    pchName: string,
    unNameBufLen: number,
  ): { result: number; pflPercent: number; pbAchieved: boolean } {
    const pflPercent_buf = scalarOut("f32");
    const pbAchieved_buf = scalarOut("bool");
    const result = this.s.SteamAPI_ISteamUserStats_GetNextMostAchievedAchievementInfo(
      this.self,
      iIteratorPrevious,
      cstrArg(pchName),
      unNameBufLen,
      pflPercent_buf,
      pbAchieved_buf,
    );
    return {
      result,
      pflPercent: readScalar(pflPercent_buf, "f32") as number,
      pbAchieved: readScalar(pbAchieved_buf, "bool") as boolean,
    };
  }

  /** Returns the percentage of users who have achieved the specified achievement. */
  getAchievementAchievedPercent(pchName: string): { ok: boolean; pflPercent: number } {
    const pflPercent_buf = scalarOut("f32");
    const ok = this.s.SteamAPI_ISteamUserStats_GetAchievementAchievedPercent(
      this.self,
      cstrArg(pchName),
      pflPercent_buf,
    );
    return { ok, pflPercent: readScalar(pflPercent_buf, "f32") as number };
  }

  /**
   * Requests global stats data, which is available for stats marked as "aggregated".
   * This call is asynchronous, with the results returned in GlobalStatsReceived_t.
   * nHistoryDays specifies how many days of day-by-day history to retrieve in addition
   * to the overall totals. The limit is 60.
   */
  requestGlobalStats(nHistoryDays: number): Promise<GlobalStatsReceived_t> {
    const call = this.s.SteamAPI_ISteamUserStats_RequestGlobalStats(this.self, nHistoryDays);
    return this.host.callResult(call, 1112, decodeGlobalStatsReceived_t);
  }

  /** Gets the lifetime totals for an aggregated stat */
  getGlobalStatInt64(pchStatName: string): { ok: boolean; pData: bigint } {
    const pData_buf = scalarOut("i64");
    const ok = this.s.SteamAPI_ISteamUserStats_GetGlobalStatInt64(
      this.self,
      cstrArg(pchStatName),
      pData_buf,
    );
    return { ok, pData: readScalar(pData_buf, "i64") as bigint };
  }

  /** Gets the lifetime totals for an aggregated stat */
  getGlobalStatDouble(pchStatName: string): { ok: boolean; pData: number } {
    const pData_buf = scalarOut("f64");
    const ok = this.s.SteamAPI_ISteamUserStats_GetGlobalStatDouble(
      this.self,
      cstrArg(pchStatName),
      pData_buf,
    );
    return { ok, pData: readScalar(pData_buf, "f64") as number };
  }

  getGlobalStatHistoryInt64(pchStatName: string, pData: bigint[], cubData: number): number {
    return this.s.SteamAPI_ISteamUserStats_GetGlobalStatHistoryInt64(
      this.self,
      cstrArg(pchStatName),
      writeScalarArray("i64", pData),
      cubData,
    );
  }

  getGlobalStatHistoryDouble(pchStatName: string, pData: number[], cubData: number): number {
    return this.s.SteamAPI_ISteamUserStats_GetGlobalStatHistoryDouble(
      this.self,
      cstrArg(pchStatName),
      writeScalarArray("f64", pData),
      cubData,
    );
  }

  /**
   * For achievements that have related Progress stats, use this to query what the bounds of that progress are.
   * You may want this info to selectively call IndicateAchievementProgress when appropriate milestones of progress
   * have been made, to show a progress notification to the user.
   */
  getAchievementProgressLimitsInt32(
    pchName: string,
  ): { ok: boolean; pnMinProgress: number; pnMaxProgress: number } {
    const pnMinProgress_buf = scalarOut("i32");
    const pnMaxProgress_buf = scalarOut("i32");
    const ok = this.s.SteamAPI_ISteamUserStats_GetAchievementProgressLimitsInt32(
      this.self,
      cstrArg(pchName),
      pnMinProgress_buf,
      pnMaxProgress_buf,
    );
    return {
      ok,
      pnMinProgress: readScalar(pnMinProgress_buf, "i32") as number,
      pnMaxProgress: readScalar(pnMaxProgress_buf, "i32") as number,
    };
  }

  /**
   * For achievements that have related Progress stats, use this to query what the bounds of that progress are.
   * You may want this info to selectively call IndicateAchievementProgress when appropriate milestones of progress
   * have been made, to show a progress notification to the user.
   */
  getAchievementProgressLimitsFloat(
    pchName: string,
  ): { ok: boolean; pfMinProgress: number; pfMaxProgress: number } {
    const pfMinProgress_buf = scalarOut("f32");
    const pfMaxProgress_buf = scalarOut("f32");
    const ok = this.s.SteamAPI_ISteamUserStats_GetAchievementProgressLimitsFloat(
      this.self,
      cstrArg(pchName),
      pfMinProgress_buf,
      pfMaxProgress_buf,
    );
    return {
      ok,
      pfMinProgress: readScalar(pfMinProgress_buf, "f32") as number,
      pfMaxProgress: readScalar(pfMaxProgress_buf, "f32") as number,
    };
  }
}
